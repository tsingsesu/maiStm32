# 电控培训记录

## 2026-09-13 I/O 端口位的基本结构

> 备注：本次内容为看图中"图13 I/O 端口位的基本结构"的知识整理。当前看不懂没关系，先记录，等后续学到 GPIO 配置时再回来看就会懂。

![图13 I/O 端口位的基本结构](学习截图/屏幕截图%202026-09-13%20210852.png)

### 一、这张图讲的是什么

这张图就是 **STM32 GPIO 引脚（一个端口位）的内部结构图**。它画的是一个引脚内部由哪些部件组成，以及：

- 外部信号怎么从引脚流进芯片内部（输入通道）
- 芯片内部信号怎么从寄存器流到引脚外面（输出通道）
- 引脚上的过压、静电怎么被保护（保护电路）

可以分成 **输入通道、输出通道、保护电路** 三大块来看。虚线框内是芯片（片内）电路，虚线框右边是接到外面的 **I/O 引脚（管脚）**。

### 二、输入通道（从引脚 → 片内）

数据方向：**I/O 引脚 → 保护二极管 → 施密特触发器 → 输入数据寄存器 → 片上外设**

1. **保护二极管（上下两个）**
   - 上面二极管接 VDD，下面二极管接 VSS。
   - 作用：当引脚电压高于 VDD 或低于 VSS 时，二极管导通，把过高/过低的电压"钳位"到电源范围内，防静电、防过压烧芯片。这是保护引脚的关键部件。

2. **TTL 肖特基触发器（图中红字标"施密特触发器"）**
   - 注意：图上写的其实是 **TTL 肖特基触发器**，但功能类似施密特触发器——带滞回特性。
   - 作用：把外部引脚上"不太干净"的模拟信号（缓慢上升/下降、带噪声）整形成干净的数字信号（0/1），并起到隔离缓冲作用。
   - 它是输入路径的核心，因为外部信号往往不理想，必须先整形。

3. **输入数据寄存器**
   - 触发器整形后的信号电平被锁存到这里，CPU 通过读操作就能读到引脚当前电平。
   - 旁路还有两路：
     - **模拟输入**：不经过触发器，直接送给片上外设（如 ADC 采集模拟量）。
     - **复用功能输入**：送给外设（如定时器输入捕获、USART 接收），不走输入数据寄存器。

### 三、输出通道（从片内 → 引脚）

数据方向：**输出数据寄存器 → 输出控制 → P-MOS / N-MOS → 引脚**

1. **输出数据寄存器**
   - 保存要输出的电平，CPU 写它就能设置引脚输出高低。

2. **位设置/清除寄存器**
   - 可以单独修改某一位，不用"读-改-写"整个寄存器。写入这里会"整体修改"输出数据寄存器的对应位。这就是它箭头指向输出数据寄存器、并标注"整体修改"的原因。

3. **输出控制 + P-MOS / N-MOS（推挽电路）**
   - **P-MOS**（上管，源极接 VDD）和 **N-MOS**（下管，源极接 VSS）组成推挽输出：
     - P-MOS 导通 → 输出高电平
     - N-MOS 导通 → 输出低电平
   - 输出控制逻辑决定工作模式，图中标注"**推挽、开漏或关闭**"：
     - **推挽**：两个管子都能导通，能主动输出高和低。
     - **开漏**：只让 N-MOS 工作，输出要么被拉低、要么高阻（需外部上拉才能出高电平）。
     - **关闭**：两个管子都关，引脚变高阻（用于输入模式）。
   - 旁边"复用功能输出"是外设直接控制输出（如 PWM 波形、USART 发送）。

### 四、其余细节

- **读 / 写 信号**：来自 CPU 总线的控制，"读出"读输入寄存器，"写入 / 读写"操作输出寄存器。
- **开关符号（开/关）**：在 VDD、VSS 与输入路径之间，用于上拉/下拉电阻和模拟开关的切换（不同模式下启用/禁用）。
- **输入驱动器 / 输出驱动器**：图中间虚线块，分别指输入侧的触发器缓冲、输出侧的 MOS 驱动级。

### 五、一句话总结

- **外部信号进芯片**：先过保护二极管钳位 → 施密特触发器整形 → 存输入数据寄存器 → 给外设或 CPU 读。
- **芯片信号出引脚**：写输出数据寄存器（或位设置/清除）→ 输出控制 → P-MOS / N-MOS 推挽或开漏驱动 → 引脚。

### 六、重点（先记住这三个）

1. **施密特触发器**负责输入整形；
2. **P-MOS + N-MOS** 决定推挽 / 开漏输出；
3. **保护二极管**负责过压保护。

这也就是为什么 GPIO 要配置成"输入 / 输出 / 复用 / 模拟"四种模式——本质上就是切换图中这些通路和开关。
