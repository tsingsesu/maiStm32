# 电控培训记录

## 2026-09-13 I/O 端口位的基本结构

> 备注：本次内容为看图中"图13 I/O 端口位的基本结构"的知识整理。当前看不懂没关系，先记录，等后续学到 GPIO 配置时再回来看就会懂。

![图13 I/O 端口位的基本结构](学习截图/屏幕截图%202026-09-13%20210852.png)

<mark>引脚电平0V~3.3V，部分可容忍5V
输出模式 控制高低电平，控制蜂鸣器
输入模式 读电平 ，按键输入


### 一、这张图讲的是什么

这张图就是 **STM32 GPIO 引脚（一个端口位）的内部结构图**。它画的是一个引脚内部由哪些部件组成，以及：

- 外部信号怎么从引脚流进芯片内部（输入通道）
- 芯片内部信号怎么从寄存器流到引脚外面（输出通道）
- 引脚上的过压、静电怎么被保护（保护电路）

可以分成 **输入通道、输出通道、保护电路** 三大块来看。虚线框内是芯片（片内）电路，虚线框右边是接到外面的 **I/O 引脚（管脚）**。

### 二、输入通道（从引脚 → 片内）

数据方向：**I/O 引脚 → 保护二极管 → 施密特触发器 → 输入数据寄存器 → 片上外设**

1. **保护二极管（上下两个）**
   - 上面二极管接 VDD，下面二极管接 VSS。
   - <mark>作用：当引脚电压高于 VDD 或低于 VSS 时，二极管导通，把过高/过低的电压"钳位"到电源范围内</mark>，防静电、防过压烧芯片。这是保护引脚的关键部件。

2. **TTL 肖特基触发器（图中红字标"施密特触发器"）**<mark>应该叫施密特？
   - 注意：图上写的其实是 **TTL 肖特基触发器**，但功能类似施密特触发器——带滞回特性。
   - <mark>作用：把外部引脚上"不太干净"的模拟信号（缓慢上升/下降、带噪声）整形成干净的数字信号（0/1），并起到隔离缓冲作用。</mark>
   - 它是输入路径的核心，因为外部信号往往不理想，必须先整形。

3. **输入数据寄存器**
   - 触发器整形后的信号电平被锁存到这里，CPU 通过读操作就能读到引脚当前电平。
   - 旁路还有两路：
     - **模拟输入**：不经过触发器，直接送给片上外设（如 ADC 采集模拟量）。
     - **复用功能输入**：送给外设（如定时器输入捕获、USART 接收），不走输入数据寄存器。

### 三、输出通道（从片内 → 引脚）

数据方向：**输出数据寄存器 → 输出控制 → P-MOS / N-MOS → 引脚**

1. **输出数据寄存器**
   - <mark>保存要输出的电平，CPU 写它就能设置引脚输出高低。

2. **位设置/清除寄存器**
   - 可以单独修改某一位，不用"读-改-写"整个寄存器。写入这里会"整体修改"输出数据寄存器的对应位。这就是它箭头指向输出数据寄存器、并标注"整体修改"的原因。

3. **输出控制 + P-MOS / N-MOS（推挽电路）**
   - **P-MOS**（上管，源极接 VDD）和 **N-MOS**（下管，源极接 VSS）组成推挽输出：
     - P-MOS 导通 → 输出高电平
     - N-MOS 导通 → 输出低电平
   - 输出控制逻辑决定工作模式，图中标注"**推挽、开漏或关闭**"：
     - **推挽**：两个管子都能导通，能主动输出高和低。
     - **开漏**：只让 N-MOS 工作，输出要么被拉低、要么高阻（需外部上拉才能出高电平）。
     - **关闭**：两个管子都关，引脚变高阻（用于输入模式）。
   - 旁边"复用功能输出"是外设直接控制输出（如 PWM 波形、USART 发送）。

### 四、其余细节

- **读 / 写 信号**：来自 CPU 总线的控制，"读出"读输入寄存器，"写入 / 读写"操作输出寄存器。
- **开关符号（开/关）**：在 VDD、VSS 与输入路径之间，用于上拉/下拉电阻和模拟开关的切换（不同模式下启用/禁用）。
- **输入驱动器 / 输出驱动器**：图中间虚线块，分别指输入侧的触发器缓冲、输出侧的 MOS 驱动级。

### 五、一句话总结

- **外部信号进芯片**：先过保护二极管钳位 → 施密特触发器整形 → 存输入数据寄存器 → 给外设或 CPU 读。
- **芯片信号出引脚**：写输出数据寄存器（或位设置/清除）→ 输出控制 → P-MOS / N-MOS 推挽或开漏驱动 → 引脚。

### 六、重点（先记住这三个）

1. **施密特触发器**负责输入整形；
2. **P-MOS + N-MOS** 决定推挽 / 开漏输出；
3. **保护二极管**负责过压保护。

这也就是为什么 GPIO 要配置成"输入 / 输出 / 复用 / 模拟"四种模式——本质上就是切换图中这些通路和开关。

### 七、补充：保护二极管的"钳位"原理

**问题：当引脚电压高于 VDD 或低于 VSS 时，二极管导通，把过高/过低的电压"钳位"到电源范围内，为什么二极管能起到这个作用？**

#### 0. 二极管单向导通原理（通俗版，结合半导体 PN 结）

核心一句话：<mark>**二极管本质就是一个 PN 结，存在"势垒（内建电场）"，外加电压必须克服这个势垒，才有大量载流子流过，表现为导通。**

**PN 结基础**

- P 型半导体：多子是空穴（带正电）。
- N 型半导体：多子是自由电子（带负电）。

两者贴在一起时：

- N 区的电子会扩散到 P 区，P 区空穴扩散到 N 区。
- 在接触面附近，电子和空穴复合消失，**形成一层没有可移动载流子的耗尽层**。
- 耗尽层里留下固定的电荷，产生**内建电场（势垒电压）**，方向：N → P。
- 这个电场会**阻止载流子继续扩散**，最后达到平衡。

#### 1. 二极管的核心特性：单向导电

二极管是一个 **PN 结**，它的伏安特性可以粗暴理解成两条：

- **正向偏置**（阳极电位比阴极高出约 0.7V）：导通，电阻很小，电流能顺畅流过，而且**导通后两端的压降几乎恒定在 0.7V 左右**（硅管），电流再大压降也基本不变。这一条是"钳位"的关键。
- **反向偏置**（阳极电位比阴极低）：截止，几乎不导电（只有极小漏电流）。

一句话：**二极管只允许电流从阳极流向阴极，且正向导通时自己"吃掉"约 0.7V 压降。**

#### 2. 看图中的接法

- **上二极管 D1**：阳极接引脚，阴极接 VDD。
- **下二极管 D2**：阳极接 VSS，阴极接引脚。

正常工作时，引脚电压 Vpin 在 VSS ~ VDD 之间：

- D1：引脚是阳极，VDD 是阴极，Vpin < VDD → **反向偏置 → 截止**。
- D2：VSS 是阳极，引脚是阴极，Vpin > VSS → **反向偏置 → 截止**。

两个都截止，所以正常信号不受影响，能自由地在 VSS~VDD 之间变化。

#### 3. 过压时发生了什么

**情况 A：引脚电压被外部拉得高于 VDD**

当 Vpin 升到 **VDD + 0.7V** 以上时，D1 变成正向偏置 → 导通。此时：

- 多余电流从**引脚 → D1 → VDD 电源轨**流走；
- D1 自己维持约 0.7V 压降，所以 Vpin 被"顶"在 **约 VDD + 0.7V**，想再往上升就得让 D1 压降继续增大，而它几乎不变。

**情况 B：引脚电压低于 VSS**

当 Vpin 降到 **VSS − 0.7V** 以下时，D2 正向偏置 → 导通。电流从 **VSS → D2 → 引脚** 流进来，把 Vpin "托"在 **约 VSS − 0.7V**。

#### 4. "钳位"这个词的含义

就像用一把钳子把电压夹住：**一旦超过范围，二极管导通，把多余电荷/能量导走，电压被限制在电源轨 ±0.7V 附近**。它不是"堵住"，而是"泄放"——把危险的高压引到电源/地，保护内部脆弱的栅极氧化层和晶体管。

#### 5. 重要提醒（考试 / 实操常考）

- **0.7V 不是绝对的**：硅二极管约 0.6~0.7V，肖特基二极管只有 0.2~0.3V。
- **二极管不是无敌的**：它能承受的电流有限。如果外部过压源没有限流电阻，二极管会被烧毁。所以实际电路常在引脚串限流电阻，或依赖芯片内部的 ESD 结构。
- **真实芯片里**这两个二极管往往不是单独做的，而是 **CMOS 晶体管的寄生体二极管（body diode）**，这也是为什么很多 MCU 引脚天然就有这种钳位能力。
- **副作用**：大量电流灌进 VDD 电源轨可能把电源电压抬高，严重时触发 **闩锁效应（latch-up）** 甚至烧芯片，所以不能把"有保护二极管"当成可以随便过压的理由。

### 八、补充：保护二极管靠"正向导通"还是"击穿"？

**问题：若电压过大，保护二极管会被击穿，电流被引走，这样起到保护作用吗？**

答案要分两层看：**图中 GPIO 这两颗靠的是"正向导通"，而"击穿"是另一回事，且分可逆/不可逆。**

#### 1. 图中这两颗靠的是"正向导通"，不是"击穿"

回顾接法：

- 上管：阳极=引脚，阴极=VDD
- 下管：阳极=VSS，阴极=引脚

引脚电压升到 **VDD+0.7V 以上**时，上管两端变成**正向偏置 → 正向导通**，电流从引脚流向 VDD，电压被夹住；引脚掉到 VSS−0.7V 以下时下管正向导通。

**这是二极管"正常工作区"的导通，不是击穿。** "击穿"指的是**反向**电压过大时发生的现象（阳极比阴极低很多），两者不是一回事。

#### 2. "击穿"分两类，必须分清

| 类型 | 别名 | 是否可恢复 | 效果 | 典型用途 |
|------|------|-----------|------|----------|
| 齐纳/雪崩击穿 | 电击穿 | **可逆**，撤压后恢复 | 电压被钳位在击穿电压 Vbr，反向电流剧增把电流引走 | 稳压管、TVS 管、ESD 保护 |
| 热击穿 | 二次击穿 | **不可逆，烧毁** | 电流太大 → 功耗 P=U×I 太大 → 结温过高 → PN 结烧穿 | 是事故，不是保护 |

- "**靠击穿把电流引走起保护作用**"——这在 **TVS / 齐纳二极管**上确实成立，但利用的是**可逆的雪崩击穿**：正常时反向截止，一旦过压就进入雪崩区，瞬间泄放大电流、钳住电压，保护后级。**TVS 管（瞬态电压抑制器）就是专门干这个的**，用于 ESD、浪涌防护。
- 若变成**热击穿**，那就是二极管被烧坏，是**损坏而不是保护**。普通二极管若无限流，反向击穿后很快热击穿而永久失效。

#### 3. GPIO 这两颗靠什么

**主要靠正向导通钳位**（不是靠反向击穿），把偶发、能量不大的过压引走。能承受的电流有限，所以：

- 实际电路常在引脚**串限流电阻**，限制流过保护二极管的电流；
- 大能量冲击（静电、浪涌）要靠外部的 **TVS 管 / ESD 保护器件**。

选 TVS 的关键参数：**击穿电压、钳位电压、峰值脉冲电流、响应速度**。

#### 4. 一句话总结（正向导通 vs 击穿）

- "电压过大就触发导通、把电流引走"——**方向对的**；
- 图中 GPIO 保护二极管靠的是**正向导通**（正常区）；
- "击穿引走电流"是 **TVS / 齐纳**的原理，且必须是**可逆的雪崩击穿**才算保护；
- 一旦电流过大变成**热击穿**，就是**烧毁**，不再是保护。

### 九、补充：导通条件与工作区间（含区间图）

**问题：为啥电压"绝对值"过大时，才会上面正向导通或下面正向导通？**

#### 1. 先纠正：不是"绝对值"过大，而是"超出电源轨"

触发导通的判据，是看每个二极管**阳极电位有没有比阴极高约 0.7V**，跟电压"绝对值"无关。之所以有"绝对值过大"的错觉，是因为常见电路里 VDD 是正的、VSS 是地或负的。

#### 2. 二极管的导通判据

> **正向导通条件：阳极电位 − 阴极电位 ≥ 约 0.7V**

代入两个二极管：

- **上管 D1**：阳极=引脚 Vpin，阴极=VDD → 导通条件 `Vpin − VDD ≥ 0.7` → **Vpin ≥ VDD + 0.7**
- **下管 D2**：阳极=VSS，阴极=引脚 Vpin → 导通条件 `VSS − Vpin ≥ 0.7` → **Vpin ≤ VSS − 0.7**

#### 3. 区间图（以 VDD = 3.3V、VSS = 0V 为例）

```
        VSS-0.7      VSS ──────────── VDD      VDD+0.7
  ────────┬───────────┼──────────────┼───────────┬────────→ Vpin
         -0.7V        0V            3.3V        4.0V

   下管导通区 │ ← 两管都截止（安全区）→ │ 上管导通区
   Vpin<-0.7  │   -0.7 ~ 4.0V          │  Vpin>4.0V
```

- **中间这段（−0.7 ~ +4.0V）**：两个二极管**都不导通**，引脚信号可自由变化，正常信号 0~3.3V 正落在此区间。
- **只有 Vpin 冲出这段区间**（比 VDD 高 0.7V 以上，或比 VSS 低 0.7V 以上）才有二极管导通。

所以准确说法是"**引脚电压偏离电源轨 0.7V 以上**"，不是绝对值大。

#### 4. 为什么正常范围内两个都不导通

因为正常范围内各自都是**反向偏置**：

- Vpin 在 VSS ~ VDD 之间时，上管阳极(Vpin) < 阴极(VDD) → 反向，截止；
- 下管阳极(VSS) < 阴极(Vpin) → 反向，截止。

二极管反向时，PN 结的**内建电势势垒（约 0.7V）**被加强，多数载流子无法越过，只有极小的漏电流。**只有外加正向电压超过这个 0.7V 势垒，载流子才能大量越过、电流才能流通**——这就是"0.7V 门槛"的由来。

#### 5. 反过来验证：和"绝对值"无关

假设电源轨是 VDD = +12V、VSS = −12V：

- 引脚哪怕摆到 **+11V 或 −11V**，两个二极管都还不导通（没超出轨 0.7V）；
- 触发点变成 Vpin > 12.7V 或 Vpin < −12.7V。

可见决定导通的是"**相对电源轨偏出多少**"，而非"电压数值大不大"。只有 VDD 与 VSS 关于 0 对称时，才看起来像"绝对值过大"。

#### 6. 一句话总结

二极管只在 **阳极比阴极高 0.7V** 时导通。上管的阴极管着 VDD，下管的阳极管着 VSS，所以引脚电压必须**往正方向冲出 VDD、或往负方向掉出 VSS 各约 0.7V**，对应二极管才分别导通——是"越过了电源轨"，不是"绝对值大"。

### 十、补充：图上为什么"看不到输入驱动器"？

**问题：这个图上怎么没有驱动器？输入通道中就直接引脚然后再到输入数据寄存器了？**

#### 1. 结论：图里有输入驱动器，它就是"TTL 肖特基触发器"

容易被标注位置误导。图中中间那两行字——**"输入驱动器"** 压在 **"输出驱动器"** 上面，分别标注上下两个虚线块：

- **输入驱动器**（上面虚线块）＝ 里面的 **TTL 肖特基触发器（施密特触发器）**。
- **输出驱动器**（下面虚线块）＝ 里面的 **输出控制 + P-MOS + N-MOS**。

所以输入通道**并不是**"引脚直接到输入数据寄存器"，完整走向是：

```
I/O引脚 ─► 保护二极管 ─┬─(上拉/下拉开关)─► 施密特触发器 ─► 输入数据寄存器 ─► 读出 ─► 片上外设
                        │                  （输入驱动器）
                        └─► 模拟输入（旁路，不经过触发器）
```

中间那个三角形带滞回符号的东西，**既是整形器，也是输入缓冲/驱动器**，只是名字写成了"TTL 肖特基触发器"，没写成"驱动器"。

#### 2. 为什么输入驱动器"不像驱动器"

关键在于输入和输出驱动器**任务不同**：

| | 输入驱动器 | 输出驱动器 |
|---|---|---|
| 任务 | **接收**外部信号、整形、隔离 | **驱动**外部负载 |
| 需不需要功率级 | 不需要 | 需要 |
| 用什么实现 | 带滞回的缓冲器（施密特触发器） | 大尺寸 P-MOS / N-MOS 推挽级 |
| 对外表现 | 高输入阻抗，几乎不取电流 | 能吐/吸较大电流 |

- **输出**要真正"驱动"外部负载（点 LED、驱动总线），必须能提供电流，所以画成显眼的 **P-MOS + N-MOS 功率级**。
- **输入**只需把外部信号干净地"接进来"送给内部逻辑，输入阻抗越高越好，所以一个**带滞回的缓冲/触发器**就够了——它"驱动"的是**片内逻辑**，不是**外部负载**。

#### 3. 顺带理清几个箭头

- **模拟输入**：从引脚节点**旁路**走，不经过触发器，直接给 ADC 等外设。
- **复用功能输入**：取触发器整形后的信号，送给定时器/USART 等外设。
- **读出**：CPU 读**输入数据寄存器**里的值。
- 那一堆 **开/关**开关和电阻，是**上拉/下拉**配置，属于输入侧的可选电路。

### 十一、补充：保护二极管与 TTL 之间的"歪歪扭扭小模块"

**问题：保护二极管和 TTL 之间那个歪歪扭扭、连着 VDD 和 VSS 的小模块有什么用？**

#### 1. 它是什么：可配置的上拉 / 下拉电阻

"歪歪扭扭"的符号是**电阻**（锯齿线是电阻的标准画法），旁边两个"**开/关**"是**受寄存器控制的开关（模拟开关/传输门）**。组成一个模块：

```
        VDD
         │
      [开/关]   ← 开关
         │
        ~~~~    ← 上拉电阻
         │
引脚节点 ┼───────► 施密特触发器
         │
        ~~~~    ← 下拉电阻
         │
      [开/关]   ← 开关
         │
        VSS
```

- **上拉支路**：电阻 + 开关接到 **VDD**。开关合上，把引脚"拉"向高电平。
- **下拉支路**：电阻 + 开关接到 **VSS**。开关合上，把引脚"拉"向低电平。
- 两个开关由 GPIO 配置寄存器控制，通常**同一时刻只开一个**（上拉 / 下拉 / 都不接，三选一）。

#### 2. 有什么用

核心作用：<mark>**当引脚是输入、且外部没有明确驱动时，给引脚一个确定的默认电平，防止"悬空"。**

1. <mark>**防止引脚悬空（浮空）**</mark>
   -<mark> CMOS 输入是高阻抗，若外部什么都不接，引脚电压会漂在中间、乱跳。
   - 悬空的后果：电平不确定 → 读到的值随机 → 内部管子处在半导通状态 → <mark>**漏电、发热、甚至振荡**</mark>。
   - 上拉/下拉把悬空引脚"定"在 VDD 或 VSS，问题就解决了。

2. **给按键/开关提供默认状态**
   - 典型<mark>按键接法</mark>：一端接引脚、一端接 GND。**不按时**靠**上拉电阻**保持高电平；**按下时**被拉到 GND 变低电平。

3. **配合开漏输出使用**
   - 开漏（只靠 N-MOS 拉低）输出高电平时是高阻，需要**上拉电阻**才能得到高电平，比如 **I²C 总线**。

#### 3. 几个要点（常考）

- **阻值较大**：通常几十 kΩ（比如 30k~50kΩ），所以叫**弱上拉/弱下拉**，只用来"定电平"，不能当电源给负载供电。
- **不能同时上拉又下拉**：否则 VDD→电阻→电阻→VSS 直接通，白白耗电（所以设计成三态）。
- **只在输入/开漏等场景有意义**：推挽输出时引脚被 MOS 主动驱动，上拉/下拉基本不起作用。
- 位置在**保护二极管之后、触发器之前**，拉的是引脚上的实际电压。

一句话：**它是"给悬空引脚定默认电平"的可开关弱电阻，防止浮空、给按键/开漏电路提供默认状态。**

### 十二、补充：输出驱动器为什么能输出高电平

**问题：输出驱动器中，为什么输出控制接到 VDD 就能输出高电平？**

#### 1. 先纠正：输出控制不是"接到 VDD"

图中 `输出控制` 这个框的输出线接到 **P-MOS 和 N-MOS 的栅极（gate）**，不是接到 VDD。真正接 VDD 的是 **P-MOS 的源极（source）**。正确说法是：

> **输出控制通过控制栅极电压，决定让 P-MOS 导通还是让 N-MOS 导通；P-MOS 一导通，就把 VDD "接"到引脚上，于是输出高电平。**

#### 2. 关键：MOS 管就是"电压控制的开关"

- **P-MOS**：源极接 **VDD**。当栅极电压比源极低足够多（栅极拉低）时**导通**——源极和漏极之间像一根很小的电阻，把 VDD 传到漏极。
- **N-MOS**：源极接 **VSS**。当栅极电压比源极高足够多（栅极拉高）时**导通**——把 VSS 传到漏极。

两者的**漏极接在一起，那个节点就是输出**，再连到 I/O 引脚：

```
        VDD
         │
      ┌──┴──┐
      │ P-MOS │◄── 栅极
      └──┬──┘
         ├───────────► I/O 引脚
      ┌──┴──┐
      │ N-MOS │◄── 栅极
      └──┬──┘
         │
        VSS
```

#### 3. 输出高电平是怎么来的

要输出**高电平**，`输出控制` 做的事是：

- **让 P-MOS 导通**（栅极拉低，低于源极 VDD）；
- **让 N-MOS 截止**（栅极拉低到 VSS，N-MOS 不通）。

P-MOS 导通后，相当于在 **VDD 和引脚之间接了一个低阻开关**，引脚被"连通"到 VDD，电压 ≈ VDD，**这就是高电平**。

要输出**低电平**则相反：让 N-MOS 导通（栅极拉高）、P-MOS 截止，把引脚连到 VSS。

#### 4. 高电平来自哪里

**来自 VDD 这个电源，通过导通的 P-MOS 流到引脚。** P-MOS 只是开关/通道，本身不产生电压。所以：

- 输出高 ≈ `VDD − I × R_on`（R_on 很小时几乎等于 VDD）；
- 输出低 ≈ `VSS + I × R_on`。

这也解释了为什么**推挽输出的高电平会被 VDD 限制住**——想输出 5V 但 VDD 只有 3.3V，引脚最高也只能到 3.3V 附近。

#### 5. 两个细节

- 图里 MOS 栅极前的**小圆圈**通常表示**反相器/非门**：保证给 P-MOS 和 N-MOS 的栅极信号**互补**，一个开另一个必关，避免两管同时导通造成 VDD 直通 VSS（**直通/穿通电流**）。
- **开漏模式**则是让 P-MOS 一直关、只留 N-MOS：只能主动拉低，高电平得靠外部/内部上拉电阻。

一句话：**输出控制不是把引脚接到 VDD，而是把 P-MOS 打开，让 VDD 经 P-MOS 送到引脚，形成高电平。**

### 十三、补充：想输出高电平，栅极驱动电平反而要低？

**问题：也就是说，要想输出高电平，输出控制这个地方的电平反而要低？**

对，**物理上确实如此**——要让引脚输出高电平，送给 **P-MOS 栅极的电压必须低**（接近 0V / VSS）。但要说清"这个地方的电平"指哪个，这是最容易绕晕的点。

#### 1. 分清两种"电平"

1. **软件里写的数据位**：往输出数据寄存器写 `1` = "我要输出高"。逻辑上是"高/1"。
2. **输出控制送到 MOS 栅极的实际电压**：这是**物理**电压。对 P-MOS 来说，要导通就得给它**低电平**。

"写 1 却要多给一个低电平"听着矛盾，其实是中间的**输出控制逻辑**（图里 MOS 栅极前的**小圆圈**就是反相/气泡符号）在做转换。

#### 2. 关键：P-MOS 是"低电平有效"（active low）

图里 P-MOS 栅极上的**小圆圈**表示：**栅极为低时它才导通**。原因：P-MOS 源极接 VDD，只有**栅极比源极低**（Vgs 为负）时才导通。所以：

- "打开 P-MOS" = 栅极拉低；
- "关掉 P-MOS" = 栅极拉到 VDD。

#### 3. 真值表（一个栅极节点同时驱动两个管子）

图里 `输出控制` 的那根线**同时**接到 P-MOS 栅极（带气泡）和 N-MOS 栅极（不带），因为两种管子互补，同一个电压就能产生相反的开/关：

| 想要输出 | 该栅极节点 | P-MOS | N-MOS | 引脚电压 |
|---|---|---|---|---|
| **高** | **低（0V）** | **导通** | 截止 | ≈ VDD |
| **低** | 高（VDD） | 截止 | **导通** | ≈ VSS |

看第一行：**栅极节点为低 → P-MOS 导通、N-MOS 截止 → 引脚被拉到 VDD → 输出高。** 这正是"想输出高，控制这里的电平反而要低"。

这其实就是一个 **CMOS 反相器**的行为：**输入低 → 输出高；输入高 → 输出低**。

#### 4. 结论

- 若指"**输出控制实际加到 MOS 栅极的电压**"：**是的，要低**，P-MOS 才会把 VDD 接到引脚。
- 若指"**写进寄存器的那个 bit**"：还是写 **1**（逻辑高），由输出控制逻辑（气泡/反相）把它变成栅极需要的低电平。

**重点/难点**：不要把"软件里的 1"和"栅极上的物理电压"混为一谈。软件层里 `1 = 输出高`，但为了让 P-MOS 导通，驱动到栅极的物理电平是低——两者之间隔着一层反相逻辑。

### 十四、补充：推挽、开漏或关闭是怎么回事

**问题：图中"推挽、开漏或关闭"是怎么回事？**

#### 1. 背景：输出级就是"上 P-MOS + 下 N-MOS"两个管子

引脚这个输出节点，往上通过 **P-MOS** 连 VDD，往下通过 **N-MOS** 连 VSS。所谓"推挽 / 开漏 / 关闭"，就是**让这两个管子哪些工作、怎么工作**的区别。

#### 2. 推挽（Push-Pull）——两个管子都用

**推** = P-MOS 把电流"推"出去（输出高），**挽** = N-MOS 把电流"拉"回来（输出低）。两个管子**互补工作**，一个开另一个关。

| 输出数据位 | P-MOS | N-MOS | 引脚 |
|---|---|---|---|
| 1（想输出高） | **导通** | 截止 | 主动拉到 **VDD**（高） |
| 0（想输出低） | 截止 | **导通** | 主动拉到 **VSS**（低） |

- 特点：**既能主动输出高、也能主动输出低**，驱动能力强（能灌能拉电流）。
- 用途：绝大多数普通 GPIO 输出，如点 LED、驱动数字信号。

#### 3. 开漏（Open-Drain）——只用下面的 N-MOS

**P-MOS 被永久关掉**，只剩 N-MOS 干活。所以它**只能主动拉低**，想要高电平必须靠**外部（或内部）上拉电阻**。

| 输出数据位 | P-MOS | N-MOS | 引脚 |
|---|---|---|---|
| 1 | 关（永久） | 截止 | **高阻**（悬空，靠上拉电阻变高） |
| 0 | 关（永久） | **导通** | 主动拉到 **VSS**（低） |

关键：开漏**输出不了真正的高电平**，只输出"低"或"高阻"，高电平是上拉电阻给的。

典型用途：

- **I²C 总线**：多个器件的 SDA/SCL 线接在一起，靠"线与"（谁拉低就是低），这时**只能用开漏**，否则两个器件一个输出高一个输出低会短路打架。
- **电平转换**：上拉电阻可以接到比 VDD 更高的电源（如 3.3V 芯片拉 5V 总线）。
- **驱动高于 VDD 的负载**：如驱动 5V 继电器，靠外部上拉到 5V。
- **多个输出共享一根线**（防止冲突）。

#### 4. 关闭 / 高阻（Off / Hi-Z，三态）——两个管子都关

P-MOS、N-MOS **全部关断**，引脚变成**高阻态（Hi-Z）**，对外既不推也不拉，等于"断开"。

| 输出数据位 | P-MOS | N-MOS | 引脚 |
|---|---|---|---|
| 任意 | 关 | 关 | **高阻** |

用途：

- 引脚配置成**输入模式**时（不能输出，避免与外部信号打架）。
- 配置成**模拟模式**时（给 ADC 用，输出级必须关掉以免干扰模拟信号）。
- 需要"不驱动这根线"的场合。

#### 5. 三者对比

| 模式 | P-MOS | N-MOS | 输出高 | 输出低 | 能否直接输出高 |
|---|---|---|---|---|---|
| **推挽** | 参与 | 参与 | 主动驱动 | 主动驱动 | **能** |
| **开漏** | 关 | 参与 | 高阻（靠上拉） | 主动驱动 | 不能（需上拉） |
| **关闭** | 关 | 关 | 高阻 | 高阻 | 不能 |

#### 6. 为什么要分这三种（重点理解）

1. **推挽驱动强，但不能两个输出接一起**：一个推高、一个拉低 → VDD 直接经两个管子短路到 VSS，电流很大、会烧管。
2. **开漏天生适合"共享线"**：它只会拉低或放开，"放开"就是高阻，不会和别人的高电平冲突，这就是 **I²C 线**的原理。
3. **关闭是输入/模拟模式的前提**：做输入或采样模拟量时，输出级必须让位，否则会干扰外部或自身。

一句话：**推挽 = 上下两个管子互补、能主动推高拉低；开漏 = 只留下面管子、只会拉低、高电平靠上拉；关闭 = 两个都关、引脚高阻。** 选哪种取决于"要不要主动输出高电平、要不要多器件共享一根线、还是这根线只做输入"。

## 2026-09-14 GPIO 初始化代码（MX_GPIO_Init）

> 备注：本次内容为看图讲解 STM32CubeMX 生成的 `MX_GPIO_Init` 代码，把前面的硬件结构知识与实际代码对应起来。

![MX_GPIO_Init 代码](学习截图/屏幕截图%202026-09-14%20145035.png)

### 一、这段代码是什么

这是 **STM32CubeMX 自动生成的 GPIO 初始化代码**（`MX_GPIO_Init`），作用是**开启 GPIO 时钟并把 PA7 配置成推挽输出、初始输出高电平**。`MX_GPIO_Init()` 会在 `main()` 里被调用一次，在程序正式干活前把 GPIO 的时钟和引脚参数配置好。名字里的 <mark>`MX_` 是 CubeMX 生成函数的固定前缀。

### 二、逐行讲解

#### 1. 函数定义与结构体

```c
static void MX_GPIO_Init(void)
{
  GPIO_InitTypeDef GPIO_InitStruct = {0};
```

- <mark>`static`：这个函数只在**本文件内**可见，外部文件不能调用，避免命名冲突。
- `GPIO_InitTypeDef`：<mark>HAL 库定义的**结构体类型**</mark>，里面装着一个引脚的各项配置（Pin / Mode / Pull / Speed / Alternate 等）。
- `= {0}`：把结构体所有成员**清零初始化**，防止结构体里有"脏数据"（<mark>未初始化的随机值</mark>）被写进寄存器。
- 注意：这里只是**定义并清零**，还没往里面填任何引脚信息。

#### 2. 用户代码保护区

```c
  /* USER CODE BEGIN MX_GPIO_Init_1 */

  /* USER CODE END MX_GPIO_Init_1 */
```

- 这是 CubeMX 生成的**注释标记**。<mark>CubeMX 重新生成代码时，**只有夹在 `USER CODE BEGIN/END` 之间的内容会被保留**，其余部分会被重新覆盖。
- **易错点**：自己写的代码一定要放在这两个标记之间，否则下次用 CubeMX 生成代码时会被清掉。

#### 3. 开启 GPIO 时钟（关键步骤）

```c
  /* GPIO Ports Clock Enable */
  __HAL_RCC_GPIOD_CLK_ENABLE();
  __HAL_RCC_GPIOA_CLK_ENABLE();
```

- STM32 的外设**默认时钟是关闭的**（为了省电）。**不开启时钟，后面所有对 GPIO 寄存器的读写都无效**——这是新手最常踩的坑，<mark>引脚怎么配都没反应，多半是忘了开时钟。
- <mark>`__HAL_RCC_GPIOA_CLK_ENABLE()`：打开 **GPIOA** 端口的时钟</mark>（下面要配 PA7，属于 GPIOA）。
- `__HAL_RCC_GPIOD_CLK_ENABLE()`：打开 **GPIOD** 的时钟。这里虽然没配 D 口引脚，但 CubeMX 根据工程里其他外设（可能用到 PD 口）一并开了。
- 规律：**用哪个端口，就要先 `__HAL_RCC_GPIOx_CLK_ENABLE()`**。

#### 4. 先设定初始输出电平（防毛刺）

```c
  /*Configure GPIO pin Output Level */
  HAL_GPIO_WritePin(GPIOA, GPIO_PIN_7, GPIO_PIN_SET);
```

- <mark>含义：往 **GPIOA 的第 7 脚**的输出数据寄存器写 **SET（高电平）**。
- **为什么写在"配置成输出"之前？** 如果先把引脚配成输出、再决定输出什么，配置完成的一瞬间引脚会输出一个**不确定的电平**（寄存器旧值），可能产生**毛刺/瞬态**，驱动继电器、蜂鸣器之类会造成误动作。**先定好电平，再开输出**，引脚一使能就是期望的稳定状态。
- <mark>`GPIO_PIN_SET` = 输出高；`GPIO_PIN_RESET` = 输出低。
- 对应硬件结构图：这一步操作的就是**输出数据寄存器**。

#### 5. 填写结构体各项参数

```c
  /*Configure GPIO pin : PA7 */
  GPIO_InitStruct.Pin = GPIO_PIN_7;
  GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
  GPIO_InitStruct.Pull = GPIO_NOPULL;
  GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;
```

逐项对应硬件结构图：

| 代码 | 含义 | 对应硬件 |
|---|---|---|
| `.Pin = GPIO_PIN_7` | 选中第 7 号引脚 | PA7 这一位 |
| `.Mode = GPIO_MODE_OUTPUT_PP` | **推挽输出**（PP = Push-Pull） | P-MOS + N-MOS 都参与，能主动推高/拉低 |
| `.Pull = GPIO_NOPULL` | **不使用上拉/下拉** | 那两个"开/关"开关都断开 |
| `.Speed = GPIO_SPEED_FREQ_LOW` | 输出**速度/翻转速率低** | 输出驱动器的边沿速率（带宽）设置 |

- **为什么推挽输出还选 NOPULL？** 因为推挽模式下引脚被 MOS **主动驱动**，<mark>上拉/下拉不起作用、也不必要</mark>（上拉/下拉主要给输入或开漏用）。<mark>和学长观点一致
- **Speed 是什么？** 它决定输出级翻转的**边沿快慢**。速度越高，信号翻转越快、能跑更高频率，但**功耗和电磁干扰（EMI）更大**。点个 LED、慢速控制用 `LOW` 足够；跑高速通信才需要调高。

#### 6. 应用配置

```c
  HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
```

- 把上面填好的结构体**一次性写进 GPIOA 的寄存器**，PA7 就正式配置完成。                                        
- `&GPIO_InitStruct` 是**取地址**（传指针），因为函数内部要读这个结构体、并通过指针把值写进寄存器。

#### 7. 第二段用户代码区

```c
  /* USER CODE BEGIN MX_GPIO_Init_2 */

  /* USER CODE END MX_GPIO_Init_2 */
}
```

- 又一处保护区，通常放**引脚初始化之后的补充代码**（比如再配别的引脚、加延时等）。

### 三、重点 / 易错点

1. **必须开时钟**：`__HAL_RCC_GPIOx_CLK_ENABLE()` 忘了，后面全白配。
2. **先 WritePin 再 Init**：避免输出毛刺。
3. **结构体 `{0}` 清零**：防止脏数据。
4. **用户代码放 `USER CODE BEGIN/END` 之间**：否则被 CubeMX 覆盖。
5. **参数与硬件对应**：`Mode=推挽`→两个 MOS 参与；`Pull=NOPULL`→上拉/下拉开关断开；`Speed`→边沿速率。

一句话：**开时钟 → 预设输出电平 → 填结构体（脚号/模式/上拉/速度）→ `HAL_GPIO_Init` 写寄存器**，PA7 就成了一个初始输出高电平的推挽输出脚。

### 四、对比学长的代码：区别在哪

![学长代码 MX_GPIO_Init](学习截图/屏幕截图%202026-09-14%20145505.png)

学长的 `MX_GPIO_Init`：

```c
void MX_GPIO_Init(void)
{
  GPIO_InitTypeDef GPIO_InitStruct = {0};

  /* GPIO Ports Clock Enable */
  __HAL_RCC_GPIOD_CLK_ENABLE();
  __HAL_RCC_GPIOA_CLK_ENABLE();

  /*Configure GPIO pin Output Level */
  HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_SET);

  /*Configure GPIO pin : LED_Pin */
  GPIO_InitStruct.Pin = LED_Pin;
  GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
  GPIO_InitStruct.Pull = GPIO_NOPULL;
  GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
  HAL_GPIO_Init(LED_GPIO_Port, &GPIO_InitStruct);
}
```

（截图里蓝色的"幽灵注释"是编辑器的注释插件，不是代码内容。）

#### 区别 1：引脚用"语义宏"而不是"裸值"（最重要）

| | 我的代码 | 学长的代码 |
|---|---|---|
| 端口 | `GPIOA` | `LED_GPIO_Port` |
| 引脚 | `GPIO_PIN_7` | `LED_Pin` |

- `LED_Pin`、`LED_GPIO_Port` 是 CubeMX 根据引脚**用户标签（User Label = LED）**在 `main.h` 里自动生成的宏：
  ```c
  #define LED_Pin        GPIO_PIN_7
  #define LED_GPIO_Port  GPIOA
  ```
- 所以**功能完全一样**（都指向 PA7），但学长写法更好：
  - **可读性**：一眼看出这是"LED 引脚"，而不是裸的 `GPIO_PIN_7`。
  - **可维护性**：以后换引脚，只需在 CubeMX 里改标签/重新生成，代码不用动；裸值则要满工程搜索替换。
  - **避免"魔法数字/魔法常量"**：`GPIOA`、`GPIO_PIN_7` 属于硬编码。
- **建议**：在 CubeMX 里给每个引脚起一个有意义的 User Label，代码里统一用宏。

#### 区别 2：`static` 有无

- 我的：`static void MX_GPIO_Init(void)`。
- 学长的：`void MX_GPIO_Init(void)`。
- `static` 表示该函数**只在本文件（main.c）内可见**，属于"内部链接"；去掉后**全局可见**，别的 .c 文件也能调用（需声明）。
- 对"只在 main.c 内部调用"的初始化函数来说，加 `static` 更严谨、避免重名冲突；学长这种写法也能正常编译运行，**不影响功能**（多为 CubeMX 版本差异或手动改动）。

#### 区别 3：输出速度 Speed 不同

| | 我的代码 | 学长的代码 |
|---|---|---|
| Speed | `GPIO_SPEED_FREQ_LOW` | `GPIO_SPEED_FREQ_HIGH` |

- **Speed 只影响输出边沿的快慢（带宽），不影响输出电平的逻辑**（不会因为选了 HIGH 就输出不同高低电平）。
- `LOW`：边沿慢，**功耗低、电磁干扰（EMI）小**，点 LED、慢速控制足够。
- `HIGH`：边沿快，能跑更高频率，但**功耗和 EMI 更大**。
- 对 LED 这种慢速负载，`LOW` 更合适；学长用 `HIGH` 也没错，只是没必要（或他之后要跑较快的翻转/PWM）。**选速度的原则：够用即可，不要盲目求快。**

#### 区别 4：用户代码保护区

- 我的代码保留了 `/* USER CODE BEGIN MX_GPIO_Init_1 */` 和 `_2` 两处保护区。
- 学长的代码里看不到这两处（可能被删掉，或截图未显示）。
- 保护区的作用：**放自己写的代码**，CubeMX 重新生成代码时会保留这些区域。删掉也能编译，但以后想加自定义代码就没有"安全区"了，容易被 CubeMX 覆盖。

#### 区别 5：注释写法

- 我的：`/*Configure GPIO pin : PA7 */`（按引脚名）。
- 学长的：`/*Configure GPIO pin : LED_Pin */`（按用户标签）。
- 只是 CubeMX 依据"有没有起标签"生成的注释不同，无功能影响。

#### 相同点（核心逻辑完全一致）

- 都用 `GPIO_InitTypeDef GPIO_InitStruct = {0};` 清零。
- 都先 `__HAL_RCC_GPIOD_CLK_ENABLE()` / `__HAL_RCC_GPIOA_CLK_ENABLE()` 开时钟。
- 都**先 `HAL_GPIO_WritePin(..., GPIO_PIN_SET)` 再 `HAL_GPIO_Init`**，避免输出毛刺。
- 都是 `Mode = GPIO_MODE_OUTPUT_PP`（推挽）、`Pull = GPIO_NOPULL`（不上拉不下拉）。

#### 一句话总结

两份代码**功能等价**，主要差别在**风格**：学长用**语义宏（`LED_Pin`/`LED_GPIO_Port`）**代替裸值，可读性和可维护性更好，是更推荐的习惯；其余差异（`static`、`Speed`、保护区、注释）不影响核心功能。

### 附：截图整理工作流约定

- 新截图默认保存在 `C:\Users\lhl87\Pictures\Screenshots`。
- 约定：需要讲解新代码/新图时，**自动把该文件夹里修改时间最新的那一张截图剪切（移动）到 `C:\Users\lhl87\Desktop\maiStm32\学习截图`**，再插入到本 md 对应章节。
- 取图规则：**只取最新一张**（避免误移无关旧截图）。
- 已处理示例：`屏幕截图 2026-09-14 145505.png`（学长代码）已从 `Pictures\Screenshots` 剪切到 `学习截图`，并插入"四、对比学长的代码"。

## 2026-09-14 中断、NVIC、EXTI

> 备注：EXTI（不是 EXIT）是"外部中断/事件控制器"。这三个是配套使用的一套：**EXTI 负责检测事件，NVIC 负责管理和排优先级，中断服务函数负责处理。**

### 一、中断（Interrupt）是什么

**CPU 正在执行主程序时，某个事件发生，CPU 暂停当前任务，转去执行一段专门的处理代码（中断服务函数 ISR），处理完再回到原来断掉的地方继续。**

- 类比：你在写作业，电话响了 → 你记住作业写到哪 → 接电话 → 接完回来接着写。
- **对比轮询（Polling）**：轮询是 CPU 不停地问"事件发生了吗？"，白白浪费时间；中断是**事件主动通知 CPU**，实时性好、不浪费 CPU。

#### 关键术语

| 术语 | 含义 |
|---|---|
| 中断源 | 谁引发的中断（GPIO、定时器、串口……） |
| 中断请求 IRQ | 中断源发出的"我要 CPU 处理"的信号 |
| 中断向量 / 向量表 | 每个中断对应的处理函数入口地址，排成一张表 |
| 中断服务函数 ISR | 中断发生时执行的那段代码（如 `EXTI0_IRQHandler`） |
| 优先级 | 谁先被处理 |
| 中断嵌套 | 高优先级中断可以打断正在执行的低优先级中断 |
| 现场保护/恢复 | 进中断前保存 CPU 寄存器，出来后恢复 |

#### 中断响应过程（重点）

1. 中断源产生**中断请求**；
2. **NVIC** 判断优先级、决定是否响应；
3. **保护现场**（寄存器压栈）；
4. 根据**中断向量表**跳到对应 **ISR**；
5. 执行 ISR（用户代码）；
6. **清除中断标志**（关键，否则会反复进中断）；
7. **恢复现场**（出栈）；
8. 返回主程序断点继续执行。

### 二、NVIC（Nested Vectored Interrupt Controller，嵌套向量中断控制器）

**它是 Cortex-M 内核的一部分，不是外设**，相当于"中断的总调度台"。

职责：

- **使能/禁止**各个中断；
- **管理优先级**；
- **支持嵌套**：高优先级打断低优先级；
- **硬件自动保护/恢复现场**（压栈出栈由硬件做，快）；
- **向量化**：按中断号直接查表跳到 ISR，响应快。

#### 优先级（重点 + 难点）

- STM32 的优先级**数值越小，优先级越高**（和直觉相反，务必记住）。
- 优先级分两段：
  - **抢占优先级（Preemption Priority）**：决定**能不能打断**别人；
  - **子优先级（Sub Priority）**：抢占级相同时决定**谁先排队**，但**不能嵌套**。
- **优先级分组**：用几位给抢占、几位给子优先级（`HAL_NVIC_SetPriorityGrouping()`，CubeMX 里可选）。
- 规则：
  - 高抢占级可打断低抢占级 → **嵌套**；
  - 抢占级相同、子优先级不同 → 不能互相打断，只按子优先级先后执行。

#### HAL 常用函数

```c
HAL_NVIC_SetPriority(EXTI0_IRQn, 1, 0);  // 抢占1，子0
HAL_NVIC_EnableIRQ(EXTI0_IRQn);          // 使能该中断
HAL_NVIC_DisableIRQ(EXTI0_IRQn);         // 禁止
HAL_NVIC_SetPriorityGrouping(...);       // 设优先级分组
```

### 三、EXTI（External Interrupt/Event Controller，外部中断/事件控制器）

**专门监测外部引脚的电平变化（边沿），决定产生中断还是事件。**

#### 1. EXTI 线怎么和引脚对应

- EXTI 有 **EXTI0 ~ EXTI15** 共 16 条线，对应**引脚号 0~15**。
- **EXTI0 可以被 PA0、PB0、PC0…… 任选一个**，靠 `SYSCFG` 寄存器选择（CubeMX 自动配）。
- 所以**同一条 EXTI 线同一时刻只能服务一个端口的一位**（PA0 和 PB0 不能同时用 EXTI0）。

#### 2. 触发方式

- 上升沿、下降沿、双边沿、软件触发。
- HAL 里对应 `GPIO_MODE_IT_RISING` / `GPIO_MODE_IT_FALLING` / `GPIO_MODE_IT_RISING_FALLING`。

#### 3. 中断 vs 事件（易混）

| | 中断（Interrupt） | 事件（Event） |
|---|---|---|
| 去向 | 触发 **NVIC** → 执行 ISR | 只产生脉冲，**触发其他外设**（ADC、TIM 等） |
| 占用 CPU | 会进中断 | 不进中断，不占 CPU |
| 用途 | 需要 CPU 处理 | 硬件联动，如定时器捕获 |

#### 4. EXTI 中断的完整流程

```
引脚电平变化 → EXTI 检测到边沿 → 置中断标志 → 通知 NVIC
→ NVIC 判优先级 → 跳到 EXTIx_IRQHandler → HAL_GPIO_EXTI_IRQHandler()
→ 清标志 + 调用 HAL_GPIO_EXTI_Callback()（用户实现）
```

#### 5. HAL 代码示例

CubeMX 把引脚配成外部中断模式后，生成：

```c
/* 在 MX_GPIO_Init 里 */
GPIO_InitStruct.Pin  = GPIO_PIN_0;
GPIO_InitStruct.Mode = GPIO_MODE_IT_FALLING;  // 下降沿触发
GPIO_InitStruct.Pull = GPIO_PULLUP;           // 按键常用上拉
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

/* 使能 NVIC */
HAL_NVIC_SetPriority(EXTI0_IRQn, 2, 0);
HAL_NVIC_EnableIRQ(EXTI0_IRQn);
```

在 `stm32fxxx_it.c`（中断文件）里，CubeMX 会生成中断入口：

```c
void EXTI0_IRQHandler(void)
{
  HAL_GPIO_EXTI_IRQHandler(GPIO_PIN_0);  // 清标志 + 回调
}
```

用户自己实现**回调**（推荐写在 `main.c` 的 USER CODE 区）：

```c
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
  if (GPIO_Pin == GPIO_PIN_0)
  {
    /* 按键处理：翻转 LED、计数等 */
  }
}
```

### 四、三者的关系（一句话理清）

```
  中断源(EXTI)         调度台(NVIC)         处理(ISR)
  检测引脚边沿   ──►   排优先级/嵌套   ──►   执行你的代码
  产生中断请求         决定何时跳转         清标志、回调
```

- **EXTI**：管"**谁**发生了什么事"（外部引脚）；
- **NVIC**：管"**何时**处理、**谁优先**"；
- **ISR/回调**：管"**怎么处理**"。

### 五、几个易错点

1. **必须清中断标志**（`HAL_GPIO_EXTI_IRQHandler` 已帮你清），否则会一直重复进中断。
2. **优先级数值越小越高**。
3. **ISR 里要尽量短**：别放延时、别做耗时运算，只置个标志、交给主循环处理。
4. **按键要消抖**：机械抖动会产生多次边沿，最好加软件消抖或用定时器。
5. **EXTI 线复用**：PA0/PB0/…共用 EXTI0，别同时用。
6. **中断里访问的全局变量要加 `volatile`**，防止编译器优化导致主循环读到旧值。

### 六、结合图19：外部中断/事件控制器（EXTI）框图详解

![图19 外部中断/事件控制器框图](学习截图/屏幕截图%202026-09-14%20162158.png)

<mark>下方那个弯弯的像月牙的是“或”门，只要有一个是1，那么结果就是1

#### 1. 这张图讲什么

前面说"EXTI 负责检测引脚边沿、产生中断或事件"，图19 就是 **EXTI 内部的具体实现**。整张图拆成三部分：**总线接口（上面）、请求产生（中间）、中断/事件输出（下面两条路）**。

#### 2. 上面：总线接口（CPU 怎么配置它）

- **AMBA APB 总线 + 外设接口 + PCLK2**：CPU 通过 APB 总线、经外设接口读写 EXTI 的寄存器。
- 从外设接口往下引出 **5 条 20 位总线**，接到各寄存器——这就是 CPU 控制 EXTI 的"操作面板"。

#### 3. 中间：寄存器（每个都有用）

| 寄存器 | 英文缩写 | 作用 |
|---|---|---|
| **中断屏蔽寄存器** | IMR | 开关：这条线的**中断**是否允许送去 NVIC |
| **事件屏蔽寄存器** | EMR | 开关：这条线的**事件**是否允许送去脉冲发生器 |
| **上升沿触发选择寄存器** | RTSR | 选择：检测**上升沿** |
| **下降沿触发选择寄存器** | FTSR | 选择：检测**下降沿** |
| **软件中断事件寄存器** | SWIER | 允许用**软件**手动触发一次请求 |
| **请求挂起寄存器** | PR | 记录"发生了请求"，**需软件写 1 清除** |

> 图中画了 5 个（EMR 在下面单独画），实际寄存器共 6 个，上表补全。

- **IMR / EMR = 两个"屏蔽开关"**：一个管中断、一个管事件，决定请求"放行"还是"拦下"。
- **RTSR / FTSR = 触发方式选择**：想检测哪种边沿就置对应位（可同时选双边沿）。
- **SWIER = 软件触发**：不靠外部引脚，程序写一下就能"假装"来了个边沿。
- **PR = 挂起标志**：请求发生了就置位并保持，**必须软件清**（对应"必须清中断标志"）。

#### 4. 信号流程：从"输入线"到"请求"

```
输入线 ──► 边沿检测电路 ──(检测到指定边沿→脉冲)──┐
        (受 RTSR/FTSR 控制)                    │
软件中断事件寄存器 ────────────────────────────►或门(OR)──►请求
请求挂起寄存器(反馈，保持) ─────────────────────┘
```

1. **输入线**（20 条）进来；
2. **边沿检测电路**根据 **RTSR/FTSR** 判断是否出现指定边沿，检测到就输出一个**脉冲**（把"电平变化"变成"脉冲"）；
3. **或门**把三种来源合成一个**"请求"**：
   - 边沿检测的脉冲；
   - 软件中断事件寄存器（软件触发）；
   - 请求挂起寄存器的反馈（一旦挂起就保持住）；
4. 请求写进**请求挂起寄存器**锁存（这就是"挂起"）。

#### 5. 下面两条路：中断 vs 事件（这张图的核心）

**路 1：中断（→ NVIC，占 CPU）**

```
请求 ──► AND(中断屏蔽寄存器 IMR) ──► 至 NVIC 中断控制器
```

- 只有 **IMR 对应位没屏蔽**，请求才能通过，送到 **NVIC** → 最终执行 ISR。
- 标志：图左侧的 **"至 NVIC 中断控制器"** 箭头。

**路 2：事件（→ 脉冲发生器，不占 CPU）**

```
请求 ──► AND(事件屏蔽寄存器 EMR) ──► 脉冲发生器 ──► 事件脉冲输出
```

- 只有 **EMR 对应位没屏蔽**，请求才通过，经**脉冲发生器**输出脉冲，去**触发别的外设**（如 ADC 启动、定时器动作）。
- **不经过 NVIC、不执行中断、不占 CPU**——这就是"事件"和"中断"的本质区别。

#### 6. 那些"20"是什么意思

每条线上标的 **"20"** 表示这是 **20 位宽**的信号（20 条 EXTI 线，一位对应一条线）。所以这 6 个寄存器都是 **20 位**的，每一位管一条 EXTI 线。

#### 7. 和前面 EXTI 讲解对应起来

- "引脚边沿 → EXTI 检测 → 置标志 → 通知 NVIC"：
  - "检测边沿" = **边沿检测电路 + RTSR/FTSR**；
  - "置标志" = **请求挂起寄存器 PR**；
  - "通知 NVIC" = **请求 AND 中断屏蔽寄存器 IMR → NVIC**。
- "中断 vs 事件"：中断走 **IMR → NVIC**；事件走 **EMR → 脉冲发生器**。
- "必须清标志"：就是清 **PR**。

#### 8. 一句话总结

**输入线经边沿检测（受 RTSR/FTSR 选择）产生脉冲，与软件触发、挂起反馈一起经或门形成"请求"，锁存到 PR；请求再分两路——经 IMR 屏蔽后送 NVIC（中断），或经 EMR 屏蔽后进脉冲发生器（事件）。**

### 七、上升沿 / 下降沿是什么，有什么区别

#### 1. 什么是"边沿"

数字信号只有两个状态：**高电平（1）** 和 **低电平（0）**。"边沿"就是**信号从一种电平跳到另一种电平的那一瞬间**——它是**过渡/瞬间**的概念，不是一段持续的电平。

```
      ┌────────┐        ┌────────┐
      │        │        │        │
──────┘        └────────┘        └──────
      ↑        ↑        ↑
    上升沿    下降沿    上升沿
   (低→高)   (高→低)   (低→高)
```

- **上升沿（Rising Edge）**：电平**从低变高**的那一瞬间（0 → 1）。
- **下降沿（Falling Edge）**：电平**从高变低**的那一瞬间（1 → 0）。

#### 2. 两者的区别

| | 上升沿 | 下降沿 |
|---|---|---|
| 跳变方向 | **低 → 高**（0→1） | **高 → 低**（1→0） |
| 常见含义 | 信号"开始/触发"、按键"松开" | 信号"结束"、按键"按下" |
| 寄存器选择 | **RTSR**（上升沿触发选择） | **FTSR**（下降沿触发选择） |

区别本质上只有**跳变方向相反**，但**触发时机不同**，用哪个取决于想在"信号变高的那一刻"还是"变低的那一刻"响应。

- 两个都允许 → **双边沿触发**（RTSR、FTSR 都置位），高低跳变都触发。
- 这就是前面 EXTI 里 **RTSR / FTSR** 的作用：选择"只认上升沿 / 只认下降沿 / 两个都认"。

#### 3. 为什么用"边沿"而不是"电平"（重点）

如果用电平触发（高电平期间一直触发），按键按住不放就会**反复/持续触发**。用**边沿触发**，只在**跳变的那一瞬间触发一次**，正好对应"事件发生了"这个动作。

所以中断/事件检测几乎都用**边沿触发**：**"变化"才是事件，"保持"不是。**

#### 4. 边沿检测电路怎么"看见"边沿

靠**比较"这一拍的信号"和"上一拍的信号"**：

- 当前 = 1，上一拍 = 0 → **上升沿**；
- 当前 = 0，上一拍 = 1 → **下降沿**；
- 两拍相同 → 没有边沿。

这正是图19 里"**边沿检测电路**"干的事：把"电平变化"变成一个**脉冲**。

#### 5. 按键例子

按键一端接引脚、一端接 GND，引脚**内部上拉**：

- 不按 → 引脚被上拉 = **高**；
- 按下 → 被拉到 GND = **低**。

于是：

- **按下瞬间** = 高→低 = **下降沿**；
- **松开瞬间** = 低→高 = **上升沿**。

所以"检测按键按下"通常配**下降沿触发**（`GPIO_MODE_IT_FALLING`）。

#### 6. 易错点

1. **机械按键有抖动**：按下瞬间电平会抖几下，产生**多个边沿**，会误触发多次，要做**消抖**（软件延时确认 / 定时器 / 硬件 RC）。
2. **边沿触发只认"跳变"**：信号一直保持高，就不会再触发。
3. **选错边沿**：想检测按下却配了上升沿，就会变成"松开才响应"。

#### 7. 一句话总结

**上升沿 = 低变高的瞬间，下降沿 = 高变低的瞬间；区别只是跳变方向，用 RTSR/FTSR 选择，边沿触发保证"只在变化那一刻响应一次"，比电平触发更适合检测事件。**

### 八、HAL_GPIO_EXTI_IRQHandler 与 HAL_GPIO_EXTI_Callback 的区别

**问题：`void HAL_GPIO_EXTI_IRQHandler(uint16_t GPIO_Pin);` 和 `void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin);` 有什么区别？为什么学长说使用时尽量用下面这个（Callback）？**

#### 1. 两个函数的角色完全不同

**`HAL_GPIO_EXTI_IRQHandler` —— 中断的"框架 / 搬运工"（HAL 库提供，你不改）**

由 CubeMX 生成的中断入口 `EXTIx_IRQHandler` 调用，源码在 `stm32f1xx_hal_gpio.c`：

```c
void HAL_GPIO_EXTI_IRQHandler(uint16_t GPIO_Pin)
{
  /* EXTI line interrupt detected */
  if (__HAL_GPIO_EXTI_GET_IT(GPIO_Pin) != 0x00u)
  {
    __HAL_GPIO_EXTI_CLEAR_IT(GPIO_Pin);   /* ① 清中断标志 */
    HAL_GPIO_EXTI_Callback(GPIO_Pin);     /* ② 转发给用户回调 */
  }
}
```

它只干两件事：**判断是这条线触发 → 清标志 → 调用回调**，是"硬件中断"和"你的代码"之间的桥。

**`HAL_GPIO_EXTI_Callback` —— 用户业务入口（`__weak` 空函数，由你覆盖）**

```c
__weak void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
  UNUSED(GPIO_Pin);
  /* NOTE: This function Should not be modified, when the callback is needed,
           the HAL_GPIO_EXTI_Callback could be implemented in the user file */
}
```

- `__weak` = **弱定义**：默认什么都不做。
- <mark>你在自己文件里写一个**同名、不带 `__weak`** 的函数，链接器就会用你的版本覆盖它，HAL 便自动调用你的。
- **你的处理逻辑就写在这里。**

#### 2. 调用关系（谁调谁）

```
EXTI0_IRQHandler            ← stm32f1xx_it.c，CubeMX 生成
   └─► HAL_GPIO_EXTI_IRQHandler(GPIO_PIN_0)   ← HAL 库：清标志 + 转发
          └─► HAL_GPIO_EXTI_Callback(GPIO_PIN_0)  ← 你写的业务代码
```

所以**不是二选一**：`IRQHandler` 必须保留（它是调用 Callback 的桥梁），你只需要**重写 Callback**。

#### 3. 为什么尽量用 Callback

1. **职责分离**：`IRQHandler` 是 HAL 的"清标志 + 转发"，`Callback` 是你的"业务"，代码清晰。
2. **不会被 CubeMX 覆盖**：`EXTIx_IRQHandler` 在 `stm32f1xx_it.c` 里由 CubeMX 生成，重新生成工程时可能被改动；<mark>而 Callback 写在 `main.c` 的 `USER CODE` 区</mark>，安全。
3. **weak 覆盖是 HAL 设计的扩展点**：写强定义即可生效，不用改库代码。
4. **不会漏清中断标志**：`HAL_GPIO_EXTI_IRQHandler` 已经帮你清标志。如果直接改 `IRQHandler` 又忘了清，会**反复进中断（死循环）**。
5. **多线共用、统一入口**：`EXTI9_5_IRQn` 一条中断管 PA5~PA9 五条线，在 Callback 里用 `GPIO_Pin` 判断是哪条，统一处理。

#### 4. 注意点

- **不要删掉 `HAL_GPIO_EXTI_IRQHandler` 的调用**，否则 Callback 永远不会被触发。
- Callback 是**所有 EXTI 共用**的，多线时要用 `if (GPIO_Pin == GPIO_PIN_x)` 区分。
- Callback 里仍要遵守"**短、快**"原则，别放延时。

#### 5. 一句话总结

**`HAL_GPIO_EXTI_IRQHandler` 是 HAL 的"中断搬运工"（清标志 → 转发），`HAL_GPIO_EXTI_Callback` 是留给用户的"业务入口"（`__weak`，可被覆盖）；学长让用后者，是把代码放在"不会被 CubeMX 覆盖、职责清晰、且不会漏清标志"的地方。**

## 2026-09-14 STM32 时钟系统（时钟树）与 HSI / HSE

![STM32CubeMX 时钟树](学习截图/屏幕截图%202026-09-14%20194237.png)

### 一、这张图是什么

它是 **CubeMX 的 Clock Configuration**，把 STM32 内部"时钟从哪来、怎么分频/倍频、送到哪去"画成一张流程图。核心思路：**时钟源 → 选源 → PLL 倍频 → 系统时钟 SYSCLK → 各级分频 → 各总线/外设**。

### 二、四个时钟源（图的左侧）

| 时钟源 | 全称 | 图中频率 | 类型 | 用途 |
|---|---|---|---|---|
| **HSI** | High-Speed Internal | 8 MHz | **内部 RC** 振荡器 | 快速启动、低成本、HSE 备份 |
| **HSE** | High-Speed External | 8 MHz（范围 1~25） | **外部晶振** | 系统主时钟，精确 |
| **LSI** | Low-Speed Internal | ~40 kHz | 内部 RC | 独立看门狗 IWDG、RTC 备用 |
| **LSE** | Low-Speed External | 32.768 kHz | 外部晶振 | RTC 精确计时 |

> 规律：**HS = High Speed（高速，给系统/CPU）；LS = Low Speed（低速，给 RTC/看门狗）；I = Internal（内部，省元件）；E = External（外部，精度高）。**

### 三、时钟怎么一步步走（对照图）

1. **选源**：PLL 源选择 HSE（8 MHz）；
2. **PLL 倍频**：×9 → **72 MHz**（8×9=72）；
3. **System Clock Mux**：选 PLLCLK → **SYSCLK = 72 MHz**；
4. **AHB 分频 ÷1** → **HCLK = 72 MHz** → 送给 **内核、内存、DMA、AHB 总线**（图右上 "HCLK to AHB bus, core, memory and DMA"）；
5. **APB1 分频 ÷2** → **PCLK1 = 36 MHz**（APB1 上限就是 36 MHz）→ 低速外设（TIM2~7、USART2/3、I2C、SPI2 等）；
   - 注意 APB1 还有个 **×2** → 定时器时钟 72 MHz；
6. **APB2 分频 ÷1** → **PCLK2 = 72 MHz**（APB2 上限 72 MHz）→ 高速外设（GPIO、USART1、SPI1、TIM1、ADC 等）；
   - APB2 定时器 ×1 → 72 MHz；
7. **ADC 分频 ÷2** → **36 MHz** → 给 ADC1/2；
8. **USB 分频** → USB 需要 **48 MHz**（F103 上是 PLL 的 1.5 分频）；
9. **FLITFCLK = 8 MHz**：Flash 接口时钟，F1 上**固定来自 HSI**；
10. **RTC 时钟选择**：从 HSE/128、LSE、LSI 里选一个给 RTC；
11. **IWDG**：由 LSI 40 kHz 供时钟；
12. **MCO**：把内部时钟（PLLCLK/2、HSI、HSE、SYSCLK）**输出到外部引脚**，用来测量或给别的芯片供时钟。

> **定时器为什么 ×2？** 当 APB 分频不为 1 时，定时器时钟会**自动×2**。比如 APB1=36 MHz（÷2），定时器就按 72 MHz 计。

### 四、重点：HSI 和 HSE 的区别

| 对比项 | **HSI**（内部高速） | **HSE**（外部高速） |
|---|---|---|
| 振荡方式 | 芯片内部 **RC 振荡器** | **外部晶振**（接在 OSC 引脚） |
| 频率 | F1 固定 **8 MHz** | 常见 **8 MHz**（范围 1~25 MHz） |
| 精度 | **较差**，RC 受温度/电压影响漂移（约 ±1%） | **很高**（晶振，ppm 级，±10~50 ppm） |
| 启动速度 | **快**（µs 级） | **慢**（ms 级，要等晶振起振） |
| 外部元件 | **不需要**（省料省 PCB） | 需要晶振 + 负载电容 + 良好布局 |
| 成本/体积 | 低、小 | 略高 |
| 典型用途 | 快速启动、低成本方案、**HSE 失效时的备份** | 系统主时钟、**USB / 精确串口波特率**等场合 |

**一句话**：**HSI 是"内部自带、省事但不够准"的 RC；HSE 是"外接晶振、麻烦但很准"的时钟。** 对精度有要求的场合（USB、串口、RTC）必须用 HSE。

### 五、为什么两个都要留着：CSS 时钟安全系统

图里 System Clock Mux 下面有个 **"Enable CSS"** 按钮。**CSS（Clock Security System，时钟安全系统）** 的作用是：

- 一旦检测到 **HSE 失效**（晶振坏、起振失败），硬件**自动切换到 HSI**，让系统继续跑、不"死机"。
- 这就是"**为什么要保留 HSI**"的现实意义——它是 HSE 的**紧急备用时钟**。

### 六、一句话总结

**时钟从源（HSI/HSE/LSI/LSE）出发，经 PLL 倍频到 72 MHz，成为 SYSCLK，再经 AHB/APB1/APB2 分频送到各总线与外设；HSI 是内部 RC（快、省、但不够准），HSE 是外部晶振（准、稳、但需外接元件），实际项目常用 HSE 做主时钟、HSI 做 CSS 备份。**

## 2026-09-14 基本定时器（图144 基本定时器框图）

![图144 基本定时器框图](学习截图/屏幕截图%202026-09-14%20195938.png)

### 一、基本定时器是什么

STM32 的定时器分三档：

| 类型 | 型号（F1） | 能力 |
|---|---|---|
| **高级定时器** | TIM1、TIM8 | 最全，带死区、刹车、互补输出，能驱动电机 |
| **通用定时器** | TIM2~TIM5 | 有输入捕获/输出比较，能测频率、输出 PWM |
| **基本定时器** | **TIM6、TIM7** | **最简单**：只有"时基单元"，无捕获/比较通道，**不能输出 PWM** |

基本定时器**只会干一件事：定时**。所以它最典型的用途就是**定时中断**，以及给 **DAC 提供触发**（图里的 TRGO）。

### 二、图144 各模块拆解

#### 1. 时钟来源

```
来自RCC的TIMxCLK ──► 内部时钟 CK_INT
```

- 时钟来自 **RCC**。TIM6/7 挂在 **APB1** 上，APB1 = 36 MHz，但**定时器时钟会 ×2 = 72 MHz**，所以 `TIMxCLK = 72 MHz`。

#### 2. 触发控制器 + 控制器

```
触发控制器 ──► TRGO ──► 至DAC
   控制器  ──► 复位、使能、计数
```

- **触发控制器**：选择时钟源、并产生 **TRGO（Trigger Output）**，可以**触发 DAC**（让 DAC 按定时节奏转换）。
- **控制器**：负责**复位、使能、计数**这些控制动作。

#### 3. 预分频器 PSC（关键）

```
CK_PSC ──► PSC 预分频器 ──► CK_CNT
```

- PSC 把输入时钟**分频**，得到计数脉冲 **CK_CNT**。
- 分频系数 = **PSC + 1**，所以：`CK_CNT = TIMxCLK / (PSC + 1)`。

#### 4. 计数器 CNT

```
CK_CNT ──► +/- CNT 计数器 ──► 停止、清除或递增
```

- 每个 CK_CNT 让 CNT **加 1**。
- `+/-` 表示计数方向，但**基本定时器只能向上计数（+）**（通用/高级才有向下、中央对齐）。

#### 5. 自动重装寄存器 ARR（决定周期）

```
         U ──► 自动重装寄存器 ──► 停止、清除或递增
                    │
                CNT 计到 ARR ──► 溢出
```

- ARR 是**计数上限**。CNT 数到 ARR 后**溢出**，产生**更新事件 U**，然后 CNT 归零重新开始。
- `(ARR + 1)` 就是**一个周期的计数次数**。

#### 6. 更新事件 U / 更新中断 UI

- **U（Update）**：溢出事件本身。
- **UI（Update Interrupt）**：如果**使能了更新中断**，U 就会触发 UI → 送到 NVIC → 执行中断服务函数。

### 三、计数流程（一句话串起来）

```
TIMxCLK ─► CK_INT ─► PSC 分频 ─► CK_CNT ─► CNT 自增
   └─► 数到 ARR ─► 溢出产生更新事件 U ─► CNT 归零 ─► 循环
                        └─（使能中断）─► UI ─► NVIC ─► 中断服务
```

### 四、定时时间怎么算（重点）

```
计数频率 f_CNT = TIMxCLK / (PSC + 1)
定时周期 T     = (PSC + 1) × (ARR + 1) / TIMxCLK
更新频率 f_UPD = TIMxCLK / [ (PSC + 1) × (ARR + 1) ]
```

**例子**：TIMxCLK = 72 MHz，取 PSC = 71、ARR = 999：

- CK_CNT = 72 MHz / 72 = **1 MHz**（每 1 µs 计一次数）；
- 一个周期 1000 个计数 → T = 1000 × 1 µs = **1 ms**。

所以"**72 MHz 时钟，要 1 ms 定时，就配 PSC=71、ARR=999**"。

### 五、影子寄存器（缓冲，易忽略）

ARR 和 PSC 都带**影子寄存器（shadow register）**：

- 改写 ARR/PSC 的值**不会立刻生效**，而是在**下一个更新事件 U** 时才载入（图中 `U ──► 自动重装寄存器` 那条线）。
- 好处：避免在计数过程中改变周期，导致当前周期被截断或错乱，保证定时稳定。

### 六、基本 vs 通用 vs 高级（一句话区分）

- **基本（TIM6/7）**：只能定时 → 产生更新中断、TRGO 触发 DAC。
- **通用（TIM2~5）**：多出**输入捕获/输出比较通道** → 测脉宽、输出 PWM。
- **高级（TIM1/8）**：再多出**死区、刹车、互补输出** → 三相电机控制。

### 七、一句话总结

**基本定时器就是"分频器 + 计数器 + 重装寄存器"三件套：TIMxCLK 经 PSC 分频得到计数脉冲，CNT 数到 ARR 溢出产生更新事件（可产生中断），从而实现精确定时；它没有 PWM 通道，最常用来定时和触发 DAC。**

## 2026-09-14 什么是 RCC

### 一、RCC 是什么

**RCC = Reset and Clock Control，复位与时钟控制器。** 它是 STM32 内部一个**专门的外设（一组寄存器）**，负责整个芯片的**时钟**和**复位**管理。可以把它理解成**全芯片的"时钟总开关 / 配电室"**——前面那张时钟树图，画的就是 RCC 内部在管的东西。

### 二、两大职责

**1. 时钟控制（Clock，最常用）**

- **管理时钟源**：HSI、HSE、LSI、LSE、PLL 的开启与选择；
- **产生系统时钟**：选 SYSCLK、分频出 HCLK / PCLK1 / PCLK2；
- **给每个外设单独使能时钟**（GPIO、TIM、USART、SPI、ADC……）；
- **CSS 时钟安全系统**（HSE 失效自动切 HSI）、**MCO 时钟输出**。

**2. 复位控制（Reset）**

- 系统复位、各外设复位、备份域复位。

### 三、为什么重要（和写过的代码直接相关）

STM32 的外设**默认时钟是关闭的**（为了省电）。**用哪个外设，就得先通过 RCC 把它的时钟打开**，否则对它的寄存器读写全都无效。

这正是 GPIO 初始化代码里那句：

```c
__HAL_RCC_GPIOA_CLK_ENABLE();   // 本质：写 RCC 的 APB2ENR 寄存器某一位
```

它干的事就是**通知 RCC 给 GPIOA 供时钟**。忘了开时钟，是新手引脚怎么配都没反应的头号原因。

### 四、相关寄存器（F1）

| 寄存器 | 作用 |
|---|---|
| `RCC_CR` | 时钟控制：开/关 HSI、HSE、PLL，就绪标志 |
| `RCC_CFGR` | 时钟配置：选时钟源、设各级分频 |
| `RCC_AHBENR` | AHB 外设时钟使能 |
| `RCC_APB1ENR` | APB1 外设时钟使能（TIM2~7、USART2/3…） |
| `RCC_APB2ENR` | APB2 外设时钟使能（GPIO、USART1、ADC…） |
| `RCC_BDCR` | 备份域：RTC、LSE |
| `RCC_CSR` | 控制/状态：复位标志、LSI |

### 五、对应 HAL 函数

```c
__HAL_RCC_GPIOA_CLK_ENABLE();   // 开 GPIOA 时钟
__HAL_RCC_TIM2_CLK_ENABLE();    // 开 TIM2 时钟
HAL_RCC_OscConfig(...);         // 配置振荡器（HSI/HSE/PLL）
HAL_RCC_ClockConfig(...);       // 配置系统时钟与分频
HAL_RCC_GetHCLKFreq();          // 读取当前频率
```

### 六、一句话总结

**RCC 就是 STM32 的"时钟与复位总管"：管时钟源、PLL、各级分频，并给每个外设单独开时钟。记住一条铁律——用任何外设之前，先经 RCC 打开它的时钟。**

## 2026-09-14 CubeMX 定时器参数：72-1 和 1000-1 是什么意思

![CubeMX 定时器参数配置](学习截图/屏幕截图%202026-09-14%20203552.png)

### 一、一句话

`72-1` 和 `1000-1` 是 CubeMX 让你**按"分频倍数 / 计数次数"来填**，而寄存器的实际值是 `72-1=71` 和 `1000-1=999`：

- **Prescaler（PSC）写 71 → 实际分频 72**
- **Counter Period（ARR）写 999 → 实际计数 1000 次**

### 二、为什么要 "-1"

因为**计数都是从 0 开始的**：

- **PSC 预分频器**：从 0 数到 PSC，一共 **PSC+1** 个时钟，才输出 1 个计数脉冲 → **分频系数 = PSC + 1**。
- **CNT 计数器**：从 0 数到 ARR，一共 **ARR+1** 个数，才溢出一次 → **一个周期 = ARR + 1 次计数**。

所以要得到"整整 72 分频""整整 1000 次计数"，寄存器里就得填 `72-1`、`1000-1`。

### 三、代入算一遍（就是 1 ms 定时）

- TIMxCLK = **72 MHz**
- CK_CNT = 72 MHz ÷ (PSC+1) = 72 MHz ÷ 72 = **1 MHz**（每 **1 µs** 一个计数脉冲）
- 定时周期 T = (PSC+1)(ARR+1) / TIMxCLK = **72 × 1000 / 72 000 000 = 0.001 s = 1 ms**
- 更新频率 = **1 kHz** → 每 **1 ms** 进一次更新中断

### 四、如果直接填 72 和 1000 会怎样

- 分频变成 73、计数变成 1001 → T = 73×1001 / 72 MHz ≈ **1.0147 ms**，不是精确的 1 ms。
- 所以**不能直接填倍数，必须减 1**。

### 五、CubeMX 的显示说明

CubeMX 允许输入**表达式**，所以它原样显示 `72-1`；生成代码时会算成 `71` 写进寄存器。直接填 `71` 效果完全一样。

### 六、顺带看其他几项

| 项 | 值 | 含义 |
|---|---|---|
| Counter Mode | Up | 向上计数（基本定时器只有这个） |
| Internal Clock Division (CKD) | No Division | 内部时钟分频，用于**滤波/采样**，不是主分频 |
| auto-reload preload | Disable | ARR 预装载关闭：改 ARR **立即生效**（不经过影子寄存器缓冲） |
| Master/Slave Mode (MSM) | Disable | 触发主从模式，与 TRGO 联动相关 |
| Trigger Event Selection | Reset (UG) | TRGO 的来源选择（这里选更新事件） |

### 七、一句话总结

**`72-1`、`1000-1` 里的 "-1" 是因为 PSC/ARR 都从 0 开始计数：实际分频 = PSC+1、实际计数 = ARR+1。要得到 72 分频和 1000 次计数，寄存器就得写 71 和 999，最终得到精确的 1 ms 定时。**

## 2026-09-14 HAL_TIM_Base_Start_IT(&htim2) 解释

### 一、一句话

`HAL_TIM_Base_Start_IT(&htim2);` = **启动 TIM2 的时基，并开启它的更新中断（Update Interrupt）**。调用之后，TIM2 开始计数，每次计数溢出就会进一次中断。

### 二、名字拆开看

| 部分 | 含义 |
|---|---|
| `HAL_TIM` | HAL 库的定时器模块 |
| `Base` | **时基单元**（PSC/CNT/ARR 这套），区别于 PWM、输入捕获等模式 |
| `Start` | 启动 |
| `IT` | **Interrupt**，表示同时**开启更新中断** |
| `&htim2` | 指向 TIM2 句柄的指针（`TIM_HandleTypeDef htim2;`，CubeMX 在 `main.c` 生成） |

### 三、它内部做了两件事

1. **开中断**：使能更新中断位（`UIE`），让"计数溢出"能产生中断；
2. **启动计数**：使能计数器（`CEN`），CNT 开始按 CK_CNT 自增。

然后硬件自动循环：**CNT 数到 ARR → 溢出 → 产生更新事件 U → 触发更新中断 UI → 进中断服务函数**。

### 四、和它长得像的几个函数（别搞混）

| 函数 | 作用 |
|---|---|
| `HAL_TIM_Base_Start(&htim2)` | **只启动计数，不开中断**（用查询标志的方式） |
| **`HAL_TIM_Base_Start_IT(&htim2)`** | **启动计数 + 开启更新中断** |
| `HAL_TIM_Base_Stop(&htim2)` | 停止计数 |
| `HAL_TIM_Base_Stop_IT(&htim2)` | 停止计数 + 关中断 |

### 五、中断触发后跑到哪

```
TIM2 计数溢出 → TIM2_IRQHandler (stm32f1xx_it.c)
   → HAL_TIM_IRQHandler(&htim2)
      → HAL_TIM_PeriodElapsedCallback(&htim2)   ← 你写的回调
```

用户回调（写在 `main.c` 的 USER CODE 区）：

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
  if (htim->Instance == TIM2)
  {
    /* 每 1 ms 执行一次：翻转 LED、计数等 */
  }
}
```

> 和前面 EXTI 一样：`HAL_TIM_IRQHandler` 是"搬运工"（清标志 + 转发），`HAL_TIM_PeriodElapsedCallback` 是留给你的"业务入口"（`__weak` 可覆盖）。多个定时器共用同一个回调，要用 `htim->Instance` 区分。

### 六、两个前提（否则中断不会来）

1. **CubeMX 里要勾选 TIM2 的 NVIC 中断使能**（生成 `HAL_NVIC_EnableIRQ(TIM2_IRQn)`），否则 `Start_IT` 只开了 UIE，NVIC 没开，中断进不来；
2. 定时器时钟要经 **RCC 使能**（`__HAL_RCC_TIM2_CLK_ENABLE()`，CubeMX 已生成在 `MX_TIM2_Init` 里）。

### 七、结合前面的配置

前面配的 `PSC=72-1`、`ARR=1000-1`，TIMxCLK=72 MHz → 每 **1 ms** 溢出一次。所以：

**`HAL_TIM_Base_Start_IT(&htim2);` 一执行，就等于"每 1 ms 调用一次 `HAL_TIM_PeriodElapsedCallback`"。**

### 八、一句话总结

**它是"启动定时器 + 打开更新中断"的开关：调用后 TIM2 开始计数，每溢出一次（本配置 1 ms）就自动进 `HAL_TIM_PeriodElapsedCallback` 回调，是定时任务最常用的起手式。**

## 2026-09-14 HAL_TIM_PeriodElapsedCallback 回调代码讲解

![HAL_TIM_PeriodElapsedCallback 代码](学习截图/屏幕截图%202026-09-14%20214835.png)

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
  if(htim->Instance == TIM2)
  {
    HAL_GPIO_TogglePin(GPIOB, GPIO_PIN_0);
  }
}
```

### 一、整体作用

这是**用户重写的"定时器更新中断回调"**：每当定时器溢出（一个周期到），HAL 就自动调用它。这段代码干的事是——**如果是 TIM2 溢出，就翻转 PB0 的电平**。

### 二、逐行拆解

#### 1. 函数头

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
```

- `HAL_TIM_PeriodElapsedCallback` 是 HAL 里的 **`__weak` 回调**，你写一个**同名**函数就覆盖它；HAL 在定时器更新中断时调用你的版本。
- 名字含义：`PeriodElapsed` = **周期到达 / 计数溢出**（更新事件）。
- 参数 `htim`：**触发这次中断的定时器句柄指针**。因为这个回调是**所有定时器共用**的，HAL 把"是谁触发的"通过它传进来。

#### 2. 判断是哪个定时器

```c
if(htim->Instance == TIM2)
```

- `htim` 是**指针**，用 `->` 访问成员。
- `htim->Instance`：句柄里保存的**定时器外设地址**（`TIM2` 是个宏，指向 TIM2 外设基地址）。
- 作用：**判断这次是哪个定时器溢出**。因为回调共用，TIM2、TIM3… 溢出都会进这里；**不判断的话，任何定时器溢出都会执行下面的代码**。
- 即使目前只用 TIM2，也建议保留这个判断，以后加了别的定时器就不容易出错。

#### 3. 翻转引脚

```c
HAL_GPIO_TogglePin(GPIOB, GPIO_PIN_0);
```

- **翻转** GPIOB 的第 0 脚（PB0）的输出电平：原来是高就变低、原来是低就变高。
- 和 `HAL_GPIO_WritePin`（写死一个电平）不同，`TogglePin` 是**取反**，适合做闪烁/翻转类动作。

### 三、实际效果

- TIM2 每 **1 ms** 溢出一次（前面 `PSC=72-1、ARR=1000-1` 的配置）→ 每 1 ms 翻转一次 PB0。
- 翻转间隔 1 ms → 一个完整方波周期 = 2 ms → **频率 500 Hz**。
- 若 PB0 接 LED：1 ms 太快，**肉眼看不到闪烁**，看起来像一直亮（偏暗）。想看"闪"通常把定时改成 500 ms（`ARR` 调大）。

### 四、注意事项

1. **回调在中断里执行，要短**：别放延时、串口打印、耗时运算。
2. **多定时器必须用 `htim->Instance` 区分**，否则会互相干扰。
3. 函数签名、名字**必须和 HAL 完全一致**，否则不会覆盖成功（链接器不报错，但你的代码永远不被调用）。

### 五、一句话总结

**这是定时器的"周期到了"回调：先判断是不是 TIM2 溢出，是就翻转 PB0。`htim->Instance` 用来区分是哪个定时器（因为回调共用），`TogglePin` 负责翻转电平。**

### 六、为什么 `if(htim->Instance == TIM2)` 能"检测 TIM2 溢出"

> 关键澄清：**`if(htim->Instance == TIM2)` 本身并不"检测溢出"**——检测溢出是 HAL 内部做的。它做的是"**判断这次中断来自哪个定时器**"。

#### 1. 完整调用链（对应 pro 工程真实代码）

```
TIM2 计数溢出
  │
  ▼
TIM2_IRQHandler()                    ← stm32f1xx_it.c:218
  │   HAL_TIM_IRQHandler(&htim2);    ← stm32f1xx_it.c:223，传入 TIM2 的句柄
  ▼
HAL_TIM_IRQHandler(htim)             ← stm32f1xx_hal_tim.c
  │   ① if ((itflag & TIM_FLAG_UPDATE) == TIM_FLAG_UPDATE)   ← 这里才"检测溢出"（读 SR 的 UIF 标志）
  │   ② __HAL_TIM_CLEAR_FLAG(htim, TIM_FLAG_UPDATE);         ← 清标志
  │   ③ HAL_TIM_PeriodElapsedCallback(htim);                 ← 把 htim **原样**传下去
  ▼
HAL_TIM_PeriodElapsedCallback(htim)  ← 你写的
      if (htim->Instance == TIM2)    ← 这里判断"是谁"
```

**关键**：HAL 在 ③ 那里，是把**刚才传进来的那个句柄**原封不动交给回调。所以"谁溢出，回调收到的就是谁的句柄"。

#### 2. `htim->Instance` 到底是什么

HAL 的句柄结构体（`stm32f1xx_hal_tim.h`）：

```c
typedef struct
{
  TIM_TypeDef  *Instance;   /*!< Register base address —— 寄存器基地址 */
  TIM_Base_InitTypeDef Init;
  ...
} TIM_HandleTypeDef;
```

- `Instance` 是一个 **`TIM_TypeDef *` 指针**，记录"这个句柄对应哪个定时器外设（基地址）"。
- 它在 `MX_TIM2_Init` 里被 CubeMX 赋值（`pro\Core\Src\tim.c:43`）：

```c
htim2.Instance = TIM2;   // 把这个句柄绑定到 TIM2 外设
```

#### 3. `TIM2` 又是什么

芯片头文件 `stm32f103x6.h`：

```c
#define TIM2_BASE   (APB1PERIPH_BASE + 0x00000000UL)
#define TIM2        ((TIM_TypeDef *)TIM2_BASE)
```

所以 `TIM2` 不是数字，而是**一个指向 TIM2 外设寄存器基地址的指针常量**。

#### 4. 于是这个比较在比什么

```c
if (htim->Instance == TIM2)
```

就是比较**两个地址**：

- 左边：**这个句柄**绑定的外设基地址；
- 右边：**TIM2** 外设的基地址。

- **TIM2 溢出** → 回调收到 `&htim2` → `htim2.Instance == TIM2` → **相等，条件成立**，执行翻转；
- **TIM3 溢出** → `TIM3_IRQHandler` 调 `HAL_TIM_IRQHandler(&htim3)` → 回调收到 `&htim3` → `htim3.Instance == TIM3 ≠ TIM2` → **不成立**，跳过。

#### 5. 为什么必须加这个判断

因为 `HAL_TIM_PeriodElapsedCallback` 是**所有定时器共用**的同一个回调（`__weak` 函数）。不管是 TIM2 还是 TIM3 溢出，最终都进这一个函数。**不加判断，任何定时器溢出都会执行里面的代码**，多个定时器就会互相干扰。所以要靠 `htim->Instance` 区分"这次是谁"。

#### 6. 一句话总结

**"检测溢出"是 HAL_TIM_IRQHandler 读 SR 的 UIF 标志做的；`htim->Instance == TIM2` 做的是"认人"——它比较句柄绑定的外设基地址与 TIM2 的基地址是否相同。因为 HAL 会把触发中断的那个句柄传给回调，所以 TIM2 溢出时这里成立、TIM3 溢出时不成立。**

## 2026-09-14 PWM 详解

### 一、PWM 是什么

**PWM = Pulse Width Modulation，脉冲宽度调制。** 说白了就是**一串高低变化的方波**，通过调节"高电平占一个周期的比例"来等效出不同的**平均电压/功率**。

它不是模拟量，但**对响应慢的负载（LED、电机）来说，效果等同于模拟量**。

```
占空比 25%：        ┌──┐        ┌──┐        ┌──┐
                   │  │        │  │        │  │
              ─────┘  └────────┘  └────────┘  └─────
                   |高|<------ 低 ------>
                   <----------- 一个周期 T --------->
```

### 二、三个核心参数

| 参数 | 含义 | 关系 |
|---|---|---|
| **周期 T** | 一个完整方波的时间 | `f = 1/T` |
| **频率 f** | 每秒多少个周期 | 越高越"平滑"，但开关损耗越大 |
| **占空比 Duty** | **高电平时间 / 周期** | `Duty = T_high / T × 100%` |
| **脉宽** | 高电平持续的时间 | 就是 `T_high` |

### 三、原理：为什么方波能等效模拟量

以 LED 为例：

- 引脚输出高电平 → LED 亮；输出低电平 → LED 灭。
- 如果**快速切换**（比如 1 kHz，人眼分辨不出），那 LED 的**平均亮度**就取决于**亮的时间比例**。
- 占空比 100% → 全亮；50% → 半亮；10% → 很暗。

对电机也一样：占空比越大，平均电压越高，转得越快。**这就是"用数字开关，做出模拟效果"**。

> 前提：负载"反应慢"（有惯性/滤波），跟不上开关速度，于是只感受到平均值。

### 四、STM32 里怎么产生 PWM

STM32 用**定时器的"输出比较"功能**来产生 PWM，涉及：

- **通用定时器（TIM2~5）** / **高级定时器（TIM1、TIM8）** 都有 PWM 输出通道（TIMx_CH1~CH4）。
- 定时器内部的 **CNT** 从 0 数到 **ARR**，循环往复（这是"周期"）。
- 再设一个 **CCR（捕获/比较寄存器）**，把它和 CNT 比较：
  - **CNT < CCR** → 输出一种电平；
  - **CNT ≥ CCR** → 输出另一种电平。

于是 CCR 就决定了"高电平占多少"——**改 CCR 就是改占空比**。

### 五、关键寄存器与计算

| 寄存器 | 作用 |
|---|---|
| **PSC** | 预分频，决定计数快慢 |
| **ARR** | 自动重装值，决定**周期（频率）** |
| **CCR** | 捕获/比较值，决定**占空比** |
| **CNT** | 当前计数值，和 CCR 比较 |

**公式（边沿对齐、向上计数）：**

```
PWM 频率  f = TIMxCLK / [ (PSC+1) × (ARR+1) ]
占空比    Duty = CCR / (ARR+1) × 100%
```

**例子**：TIMxCLK = 72 MHz，PSC = 71，ARR = 999：

- 计数频率 = 72 MHz / 72 = 1 MHz；
- PWM 频率 = 1 MHz / 1000 = **1 kHz**；
- 若 CCR = 250 → 占空比 = 250/1000 = **25%**。

> 注意：**ARR 决定频率，CCR 决定占空比**。改频率要动 ARR/PSC，改亮度/转速只动 CCR。

### 六、PWM 模式 1 / 模式 2（易混）

以**向上计数**为例：

| 模式 | CNT < CCR | CNT ≥ CCR |
|---|---|---|
| **PWM 模式 1** | **有效电平**（默认高） | 无效电平（低） |
| **PWM 模式 2** | 无效电平（低） | **有效电平**（默认高） |

- 通常用 **PWM 模式 1**：CCR 越大，高电平时间越长，占空比越大，LED 越亮/电机越快。
- "有效电平"的高低还可以用**极性**再翻转（`Polarity`）。

### 七、HAL 代码示例

CubeMX 里把某通道配成 **PWM Generation CHx**，生成后：

```c
/* 启动 PWM 输出（指定通道） */
HAL_TIM_PWM_Start(&htim2, TIM_CHANNEL_1);

/* 运行时修改占空比（改 CCR） */
__HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, 500);  // 50% (ARR=999)

/* 也可以改频率（改 ARR） */
__HAL_TIM_SET_AUTORELOAD(&htim2, 1999);
```

**引脚**：PWM 从定时器的通道引脚输出，需要把对应 GPIO 配成**复用推挽输出**（CubeMX 自动配好）。

### 八、常见应用

1. **LED 调光**：调占空比改亮度。
2. **电机调速**：调占空比改平均电压 → 改转速。
3. **舵机控制**：靠**固定频率 + 特定脉宽**（50 Hz、0.5~2.5 ms 脉宽）定角度。
4. **DAC 替代 / 音频**：配合 RC 滤波，把 PWM 变成模拟电压。
5. **开关电源 / 加热控制**：调功率。

### 九、易错点

1. **频率和占空比要分清**：ARR/PSC 管频率，CCR 管占空比。
2. **CCR 不要超过 ARR**：超过就一直是 100% 占空比。
3. **引脚要配成复用功能**，否则 PWM 出不来。
4. **频率太低会闪/抖**：LED 调光建议 >1 kHz，电机驱动几十 kHz 常见。
5. **高级定时器还要使能主输出**：TIM1/TIM8 需要 `__HAL_TIM_MOE_ENABLE()`（或 `HAL_TIM_PWM_Start` 内部处理），否则互补输出不工作。

### 十、一句话总结

**PWM 就是"可调占空比的方波"：CNT 从 0 数到 ARR 形成周期（决定频率），CNT 与 CCR 比较决定高电平长度（决定占空比）；对 LED、电机这类慢负载，占空比等效于平均电压，所以用数字开关实现了模拟控制。**

## 2026-09-14 CubeMX PWM Generation Channel 1 参数配置

![PWM Generation Channel 1 参数配置](学习截图/屏幕截图%202026-09-14%20222145.png)

> 备注：本次是 CubeMX 里把定时器某个通道配成"PWM 输出"时的参数面板（PWM Generation Channel 1），和前面"PWM 详解""72-1 / 1000-1 是什么意思"两节直接对应——就是把那两节讲的寄存器（ARR / CCR / 极性……）在图形界面上逐项填好。

### 一、这张图是什么

它是 **CubeMX 定时器配置里"PWM 通道 1"（Channel 1）的参数页**，作用是把前面"PWM 详解"里的寄存器（CCR、极性、预装载等）用图形界面一项项配好。这一页配完后，CubeMX 会生成 `HAL_TIM_PWM_ConfigChannel(...)` 那段代码。

面板共 5 项：**Mode、Pulse、Output compare preload、Fast Mode、CH Polarity**。

### 二、逐项解释

#### 1. Mode = PWM mode 1（PWM 模式 1）

- 含义：**向上计数时，`CNT < CCR` 输出有效电平，`CNT ≥ CCR` 输出无效电平**（前面"PWM 详解 第六节"讲过）。
- 效果：**CCR 越大 → 高电平时间越长 → 占空比越大**（LED 越亮 / 电机越快），符合直觉，是最常用的模式。
- 对应代码：`sConfigOC.OCMode = TIM_OCMODE_PWM1;`

#### 2. Pulse (16 bits value) = 500-1（脉宽，就是 CCR）

- 这一项就是往 **CCR（捕获/比较寄存器）** 里写的值，**决定占空比**。"16 bits" 表示它是个 16 位寄存器（取值 0~65535）。
- CubeMX 允许输入**表达式**，所以 `500-1` 原样显示；生成代码时会算成 **499** 写进去（`sConfigOC.Pulse = 499;`）。
- **占空比公式**（边沿对齐、向上计数）：

  ```
  Duty = CCR / (ARR + 1)
  ```

  前面 Counter Settings 里配的是 `ARR = 1000-1 = 999`，所以 `ARR + 1 = 1000`：

  - `CCR = 499` → Duty = 499 / 1000 = **49.9%**（≈ 50%）
  - 想精确 50%，应填 **500**（Duty = 500 / 1000 = 50%）

- **关于这个 `-1`（重点 / 易错）**：
  - **PSC、ARR 因为"从 0 开始计数"，确实要 `N-1`；但 Pulse（CCR）不遵循这个规律**，它直接就是比较值，没有"再减 1"的说法。
  - 所以这里的 `500-1` 是把"PSC/ARR 都要减 1"的习惯带过来了，实际得到 499（≈50%），**差别很小，但严格说不是精确 50%**。
  - 正确算法：**`CCR = 想要的占空比 × (ARR + 1)`**。50% → 500，25% → 250，10% → 100。
- 对应代码：`sConfigOC.Pulse = 499;`（运行时可用 `__HAL_TIM_SET_COMPARE()` 改）

<mark>**不是电平和 CCR 比，是定时器计数值 `CNT` 和 `CCR` 做比较！**
CNT = 计数器不断往上累加的数字，不是电压电平。

> 
> 配置：向上计数（Counter Mode = Up），PWM Mode1，CH 极性 High（有效电平 = 高电平）
> 规则：
> 
> 
> 1. **CNT < CCR** → 输出【有效电平（高）】
> 2. **CNT ≥ CCR** → 输出【无效电平（低）】

#### 3. Output compare preload = Enable（输出比较预装载）

- 含义：**CCR 也带影子寄存器（预装载寄存器）**。Enable 表示：**写入 CCR 的新值不会立刻生效，而是在下一个更新事件（UEV，即 CNT 溢出）时才载入**。
- 好处：避免在**一个 PWM 周期中途**改变占空比，导致出现一个"半截"的不完整波形（**毛刺 / 波形不对称**）。
- Disable 则是**立即写入、立即生效**（当前周期就可能被改掉）。
- 一般**建议 Enable**，改占空比时波形更干净。对应寄存器位 **OCxPE**。
- 和前面定时器讲的 **ARR 影子寄存器**同一个道理，只是这里管的是 **CCR**。

#### 4. Fast Mode = Disable（快速模式）

- 含义：**输出比较的"快速使能"（OCxFE）**，让输出对**触发输入（TRGI）**以**最小延迟**响应（跳过同步 / 滤波带来的延时）。
- 它主要用于"外部触发要立刻改变输出"的特殊场景（如单脉冲模式、触发联动），**普通 PWM 用不到**。
- 所以这里保持 **Disable** 即可。对应代码：`sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;`

#### 5. CH Polarity = High（通道极性）

- 含义：规定**"有效电平"到底是高还是低**。
- **High = 有效电平为高电平**。配合 PWM 模式 1：`CNT < CCR` 时输出**高**，占空比就等于高电平占比，符合常规理解。
- 若改成 **Low**：有效电平变成低，波形**整体反相**（占空比的"高低"含义也跟着反过来）。
- 对应代码：`sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;`

### 三、和前面几节串起来

- **Counter Settings 里的 PSC / ARR** 决定 **PWM 频率**；本页的 **Pulse（CCR）** 决定 **占空比**。
- 本配置：TIMxCLK = 72 MHz、PSC = 72-1、ARR = 1000-1 → 计数 1 MHz、**PWM 频率 = 1 kHz**；Pulse ≈ 500 → **占空比 ≈ 50%**。
- 生成代码会出现在 `MX_TIMx_Init` 里：

```c
sConfigOC.OCMode = TIM_OCMODE_PWM1;
sConfigOC.Pulse = 499;
sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
HAL_TIM_PWM_ConfigChannel(&htimx, &sConfigOC, TIM_CHANNEL_1);
```

- 启动输出、运行时改占空比：

```c
HAL_TIM_PWM_Start(&htimx, TIM_CHANNEL_1);            // 启动 PWM 输出
__HAL_TIM_SET_COMPARE(&htimx, TIM_CHANNEL_1, 500);   // 改占空比（改 CCR）
```

### 四、一句话总结

**这一页就是"给 PWM 通道 1 逐项填寄存器"：Mode 选模式 1（CCR 越大占空比越大）；Pulse 填的就是 CCR，`500-1` 实际是 499、占空比 ≈50%（要精确 50% 应填 500，Pulse 不像 PSC/ARR 那样需要减 1）；Output compare preload 让新 CCR 在周期结束（更新事件）时才生效，防止波形出现毛刺；Fast Mode 普通 PWM 用不到、保持 Disable；CH Polarity=High 表示有效电平为高。**

## 2026-09-14 CubeMX 生成的 PWM 通道配置代码（TIM_OC_InitTypeDef 与 HAL_TIM_PWM_ConfigChannel）

> 备注：本次是 CubeMX 在 `MX_TIM2_Init()` 里自动生成的 **PWM 通道配置代码**，正好对应上一节"PWM Generation Channel 1 参数配置"面板——面板里填的每一项，都会变成下面的一行赋值。（按约定本次不插图，直接转成代码文字讲解。）

```c
sConfigOC.OCMode = TIM_OCMODE_PWM1;
sConfigOC.Pulse = 500;
sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
if (HAL_TIM_PWM_ConfigChannel(&htim2, &sConfigOC, TIM_CHANNEL_1) != HAL_OK)
{
  Error_Handler();
}
```

### 一、这段代码是什么

它是 **CubeMX 为"TIM2 的通道 1"生成的一段 PWM 配置代码**，位于 `Core/Src/tim.c` 的 `MX_TIM2_Init()` 函数里。作用：把上一节面板里选的参数（PWM 模式 1、Pulse=500、极性 High、Fast Mode 关）**逐项填进一个结构体，再一次性写进 TIM2 通道 1 的寄存器**。

代码套路和我们前面见过的 GPIO 初始化**完全一样**：**先填结构体 → 再调用 HAL 函数写寄存器**。

### 二、逐行拆解

#### 1. 先看结构体定义（截图外，补全背景）

这段代码用到的 `sConfigOC` 是在函数开头定义的：

```c
TIM_OC_InitTypeDef sConfigOC = {0};
```

- `TIM_OC_InitTypeDef`：HAL 定义的结构体类型，专门装**输出比较 / PWM 通道**的配置（Mode、Pulse、Polarity、FastMode 等）。
- `sConfigOC`：变量名，`s` = struct、`Config` = 配置、`OC` = Output Compare（输出比较）。
- `= {0}`：把全部成员清零，防止"脏数据"（未初始化的随机值）被写进寄存器，和 `GPIO_InitStruct = {0}` 一个道理。

#### 2. 四行赋值（一一对应面板的四项）

| 代码 | 含义 | 对应面板项 | 对应寄存器位 |
|---|---|---|---|
| `sConfigOC.OCMode = TIM_OCMODE_PWM1;` | 输出比较模式 = **PWM 模式 1** | Mode = PWM mode 1 | CCMR1 的 OCxM |
| `sConfigOC.Pulse = 500;` | 比较值 **CCR = 500**，决定占空比 | Pulse = 500 | CCR1 |
| `sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;` | 有效电平 = **高** | CH Polarity = High | CCER 的 CCxP |
| `sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;` | 快速模式 = **关闭** | Fast Mode = Disable | CCMR1 的 OCxFE |

- 这里写的是 `Pulse = 500`（不是上一张图的 `500-1`）：因为 `ARR + 1 = 1000`，所以占空比 = 500 / 1000 = **精确 50%**。
- `TIM_OCMODE_PWM1`、`TIM_OCPOLARITY_HIGH`、`TIM_OCFAST_DISABLE` 都是 HAL 定义好的**宏常量**（可读性比直接写数字好，编译时替换成对应数值）。

#### 3. 关键的一句：把配置写进寄存器

```c
if (HAL_TIM_PWM_ConfigChannel(&htim2, &sConfigOC, TIM_CHANNEL_1) != HAL_OK)
```

三个参数：

| 参数 | 含义 |
|---|---|
| `&htim2` | 目标定时器句柄（TIM2），`&` 取地址 |
| `&sConfigOC` | 上面填好的配置结构体 |
| `TIM_CHANNEL_1` | 指定配置**通道 1**（还有 `TIM_CHANNEL_2/3/4`） |

- `HAL_TIM_PWM_ConfigChannel` 干的事：**把结构体里的值逐项写进 TIM2 通道 1 的相关寄存器**（CCMR1、CCER、CCR1），并做一些参数校验。
- 返回值是 `HAL_StatusTypeDef`：成功 `HAL_OK`，参数非法等返回 `HAL_ERROR`。

#### 4. 错误处理

```c
{
  Error_Handler();
}
```

- `!= HAL_OK` 表示"配置失败"，失败就调用 `Error_Handler()`。
- `Error_Handler()` 是 CubeMX 生成的函数（在 `main.c`），里面通常是一个**死循环 + 关中断**：让程序停在出错处方便调试，而不是带着错误继续跑。
- 这种"**调用 → 判断返回值 → 失败进 Error_Handler**"的写法是 HAL 库的标准风格，前面 `HAL_TIM_Base_Init`、`HAL_GPIO_Init` 等处也是同样的套路。

### 三、这段代码在 `MX_TIM2_Init()` 里的位置

完整顺序大致是：

```c
void MX_TIM2_Init(void)
{
  TIM_ClockConfigTypeDef sClockSourceConfig = {0};
  TIM_MasterConfigTypeDef sMasterConfig = {0};
  TIM_OC_InitTypeDef sConfigOC = {0};          /* ← 本段用到的结构体 */

  htim2.Instance = TIM2;
  htim2.Init.Prescaler = 72-1;
  htim2.Init.Period = 1000-1;
  ...
  HAL_TIM_Base_Init(&htim2);                   /* 先配时基（PSC/ARR） */
  HAL_TIM_ConfigClockSource(&htim2, &sClockSourceConfig);
  HAL_TIM_PWM_Init(&htim2);
  ...
  sConfigOC.OCMode = TIM_OCMODE_PWM1;          /* ← 本次讲的部分 */
  sConfigOC.Pulse = 500;
  sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
  sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
  HAL_TIM_PWM_ConfigChannel(&htim2, &sConfigOC, TIM_CHANNEL_1);

  HAL_TIM_MspPostInit(&htim2);                 /* 配置 PWM 输出引脚（复用推挽） */
}
```

- <mark>**先 `HAL_TIM_Base_Init` 配好 PSC/ARR（决定频率），再 `HAL_TIM_PWM_ConfigChannel` 配通道（决定占空比 / 极性）**，顺序不能反。
- 这段代码**只是"配置"**，并不会让引脚开始输出波形。

### 四、配置完还要启动（重点 / 易错）

<mark>配置 ≠ 输出。要让 PWM 真正跑起来，还必须在 `main()` 里调用：

```c
HAL_TIM_PWM_Start(&htim2, TIM_CHANNEL_1);   /* 使能通道输出 */
```

之后引脚才会输出 **1 kHz、占空比 50%** 的方波。<mark>运行时想改占空比：

```c
__HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, 250);  /* 改成 25% */
```

### 五、易错点

1. **`Pulse` 是 CCR，不要按 PSC/ARR 的"减 1"习惯写**：要 50% 就填 500（`Duty = CCR / (ARR + 1)`）。
2. **`ConfigChannel` 只是配置，不是启动**：忘了 `HAL_TIM_PWM_Start` 会"配好了但没波形"。
3. **通道号要对**：`TIM_CHANNEL_1` 要和 CubeMX 里启用的通道、以及引脚对应，否则会配到别的通道上。
4. **别忘了 `HAL_TIM_MspPostInit`**：它负责把引脚配成**复用推挽输出**，没有它波形出不来（CubeMX 自动生成，不要手删）。
5. **结构体先 `{0}` 再赋值**：防止未初始化成员被写进寄存器。

### 六、一句话总结

**这段代码就是把上一节面板的选项"翻译"成寄存器：用 `sConfigOC` 装好 PWM 模式 1、CCR=500（50% 占空比）、极性 High、Fast Mode 关，再交给 `HAL_TIM_PWM_ConfigChannel(&htim2, &sConfigOC, TIM_CHANNEL_1)` 一次性写进 TIM2 通道 1 的寄存器；返回值不为 `HAL_OK` 就进 `Error_Handler()`。注意它只负责"配置"，真正输出波形还要再调用 `HAL_TIM_PWM_Start`。**

## keil

![keil界面](学习截图/屏幕截图%202026-09-15%20001459.png)

### Keil MDK 工具栏这一排按钮，从左到右逐个讲解

1. **📄编译当前文件（Translate，仅编译打开的这一个.c 文件）**
快捷键 F7，**只编译你当前打开的单个文件**，不链接整个工程。

> 
> 适合快速看当前代码语法有没有错，不会生成可下载的 hex/axf。

2. **🔽Build（Build target files，增量编译，F7）****编译目标工程，只编译改动过的文件**，没修改的文件不会重新编译，速度快。
编译 + 链接，生成最终可下载程序。日常写代码反复调试首选这个。
3. **🔽🔽Rebuild（Rebuild all target files，全部重新编译）****全部文件删掉缓存，从头到尾完整重新编译整个工程**。
改了头文件、宏、CubeMX 重新生成代码后，用这个，防止旧缓存带来奇怪 bug，速度慢一点。
4. **📦批量打包按钮（Dependencies，展开下拉菜单）**
一般很少点，用来查看工程文件依赖关系。
5. **🧹清理（Clean）**
删除编译产生的所有中间文件、obj 缓存。相当于手动清垃圾。一般 rebuild 内部会自动做清理。
6. **⚡LOAD（Download / 下载烧录）****把编译好的程序下载烧录进 STM32 单片机**（需要 ST-Link/J-Link 调试器接好板子）。
✅你写完代码，Build 无报错后，点这个，程序就写到芯片里面跑。