# 视觉-硬件开发 Ver0904

> 记录日期：2026-09-04
> 背景：2025 江苏省机器人大赛 · 服务机器人「人、物辨识赛」· 视觉组
> 本文整理：比赛规则速览、maiVision 项目现状、开发板选型讨论、双板架构决策、合规性与注意事项

---

## 一、比赛规则速览（人、物辨识赛）

### 1.1 竞赛目标
服务机器人「人、物辨识赛」针对日常生活场景，测试服务机器人的**移动能力**与**基于视觉的物体检测及辨别能力**。

### 1.2 比赛环境
- 冷光源、50Hz 光照；图片分辨率 **1280×720**
- 场地：两张白桌（2m×0.74m）一前一后，纯白桌布铺满，背景白色摄影棚布，桌后有人群（m 人，或站或坐，面部朝向机器人）
- 桌上摆放 n 个物品（可能重叠但不完全遮挡），另有随机障碍物
- 表格：桌子前后为"拍摄区"，桌子与桌子之间有入镜人员

### 1.3 识别对象与评分（核心）
| 任务 | 得分 |
|---|---|
| 正确框选入镜人员脸部 | 5 分 × m 个 |
| 正确框选前提下标注出性别（male/female） | 5 分 × (m-1) 个 |
| 正确框选前提下，**不在数据集**的入镜人员标 unfamiliar | 5 分 × 1 个 |
| 正确框选前提下，标注出名字（英文名） | 5 分 × (m-1) 个 |
| 正确框选物品 | 5 分 × n 个 |
| 正确框选前提下，标注正确英文名称 | 5 分 × a 个（a=集内物品） |
| 正确框选前提下，集外物品标 unfamiliar | 5 分 × b 个（b=集外物品，n=a+b） |

**扣分项：**
- 每出现一次错误框选：**扣 2 分**（方框嵌套、一个物品多个框/标签、一个框含多个物品均算错误框选，按涉及物品数量计）
- 每将环境之外的东西框选一次：**扣 5 分**
- 机器人触碰桌子、道具、障碍物一次：**扣 10 分**
- 车轮未进入规定区域 / 触碰胶带，每轮出界或碰胶带一个车轮：**扣 5 分**

**重要细节：**
- 标注错误不得分，必须按标准标注（官方英文标签）
- 物品类目：食品（cookies/crisps/instant noodles/…）、饮品（milk/water/coffee/…）、日用品（shampoo/soap/toothpaste/…），每类下还有子类
- 人名参考（比赛前公布）：Daniel/丹尼尔、Michael/迈克尔、Jack/杰克、Fisher/费希尔、Kevin/凯文、Rose/露丝、Jhon/约翰、Mary/玛丽、Adam/亚当、Tom/汤姆 等 10 人

### 1.4 比赛流程与规则
- **两轮**：第一轮定点识别（放在识别区直接识别）；第二轮避障+识别（入场后机器人自行驶入起点区，赴到达区识别）
- 机器人必须**自主移动**：需要具备导航避障能力、人脸识别+名字、性别、物品识别
- 入场需通过门道（**高 180cm、宽 70cm**），通过方有比赛资格；机器人最大高度 1.8m、最大重量 150kg
- 2 分钟准备：调试时间内机器人不得离开起点区，超时未示意开赛计 0 分；**示意后除启动外不得再操作机器人**（不能碰代码），否则 0 分；入镜人员示意后立刻就位
- 启动到识别完成限时 **3 分钟**，超时立刻终止计分
- 输出要求：识别结果以图片格式（带颜色无填充方框+上方物品/人名称）显示在终端，**同时现场发送邮件到裁判邮箱**；报告文件命名：抽签序号-学校-队名.xxx
- 同分时按用时排名，用时少者靠前

### 1.5 资格认证（比赛前一周，别忘！）
提交到 js_servicerobot@163.com，压缩包命名：XX单位_队伍名称_赛项名称_资格认证材料，需包含：
1. 队伍介绍（word/pdf，宋体小四、1.5 倍行距）
2. 机器人功能展示视频（**<100MB**）
3. 机器人介绍材料（**≥4 页**，注明机器人型号、控制板、电机驱动器型号、硬件设备与软件技术；同一单位多队须体现技术方案差异性）

---

## 二、maiVision 项目现状（前期准备）

### 2.1 已跑通的全链路
`拍照片 → Label Studio 标注 → 导出 → 整理到 yolo_train → pipeline.py(缩放+增强) → 训练`

- 模型：yolov8s，已跑通"拍图→标注→训练→摄像头实时识别"
- 当前只做**奥利奥一个类**（`aoliao_heiqiao`）

### 2.2 关键资产
| 文件/资产 | 作用 | 状态 |
|---|---|---|
| maitrain.py | 训练脚本（yolov8s，100 轮，patience=30，seed=42） | ✅ |
| pipeline.py | 数据流水线（统一缩放到 1280×720 + 每图 6 倍增强：翻转/旋转/变亮/变暗/缩放） | ✅ |
| detect_oreo.py | 摄像头实时识别（加载 train-3/best.pt，conf=0.6） | ✅ |
| maidata.yaml | 数据配置（train=images/train_aug 约 180 张，val=官方 5 张，names={0: aoliao_heiqiao}） | ✅ |
| runs/detect/train-3/ | **最佳模型：weights/best.pt**（第 41 轮，第 71 轮早停） | ✅ |
| maiAnswer.md / 学习问题记录.md | 学习笔记 | ✅ |

### 2.3 当前最好成绩（train-3，yolov8s）
- **mAP50 = 0.995，mAP50-95 = 0.90**，P=0.99，R=1.0（第 41 轮）
- train/cls_loss 从 7.96 → 0.35（收敛健康）

### 2.4 已知短板（比赛导向，已讨论）
1. **验证集只有 5 张图** → 指标抖动大（曾有轮次 P/R/mAP 全 0，不是模型坏了）
2. **背景误检 3 次**（confusion matrix 的 background 列）
3. **边缘盲区**：labels.jpg 显示奥利奥全在画面中央，模型没练过边缘 → 边缘漏检
4. **分布偏移**：官方棚拍训练 vs 摄像头实测，效果暴跌（结论：验证集高分 ≠ 实战好）

**应对三件套（按性价比）：** ① 限制识别区域（只检测画面中央）② 置信度阈值调 0.7~0.8 ③ 多帧投票（连续多帧都检到才输出）

### 2.5 血泪总结（以后还会踩）
1. 数据太少 → 过拟合：30 张不够，增强到 180 张才收敛
2. 背景单一 → 模型记背景不记物体：要换背景拍
3. 图片尺寸不统一 → 学不会：统一 1280×720
4. 训练/验证场景差异大 → 分布偏移
5. Windows 训练必须 `if __name__ == '__main__':`（否则 DataLoader 多进程报错）
6. opencv-python-headless 会抢占 cv2 → 卸载恢复完整版
7. Label Studio 用 `127.0.0.1:8080`，不用 localhost（IPv6 解析问题）

### 2.6 环境
- Python 3.13；torch 2.11 + cu128（RTX 5060 Laptop 8GB）；ultralytics 8.4.116；opencv 4.10

---

## 三、开发板选型讨论（今日核心）

### 3.1 目标
- 正式比赛学长推荐：**树莓派 4B**
- 现学习阶段想买**轻量便宜**的板子，学完能**无缝衔接**到正式板子

### 3.2 判断标准："无缝衔接"的本质 = 三层一致
| 维度 | 影响 | 风险 |
|---|---|---|
| 1. CPU 架构（ARM64 vs x86） | pip 装的包（opencv/torch）通不通用 | 选 ARM 架构板 → 零成本 |
| 2. 操作系统（Debian/Ubuntu 系） | apt 命令、系统结构 | 都是 Debian 系 → 零成本 |
| 3. 外设接口库（GPIO 电机控制） | 树莓派用 gpiozero/pigpio，其他板用 wiringPI 系 | ⚠️ **唯一真正的缝隙** |

**两个降低门槛的关键事实：**
1. 视觉链路（detect_oreo.py）用的是 **USB 摄像头（cv2.VideoCapture）+ YOLO + 输出图片**，USB 摄像头在 Linux 上全平台通用（V4L2）→ 单纯跑视觉，第 3 点不构成障碍
2. 比赛是"拍照 → 识别 → 输出图片"，**不是实时视频**，限时 3 分钟 → 单张推理 3~5 秒完全够用 → **不需要买贵的板子**，这是"先买便宜的"能成立的前提

### 3.3 四个候选方案对比

| 方案 | 价格 | 优点 | 缺点 | 适用 |
|---|---|---|---|---|
| **A. 树莓派 Zero 2W** | 约 130~160 元 | OS 与 4B 一模一样；GPIO 库、教程、踩坑经验 100% 复用（真·无缝） | **仅 512MB 内存**：yolov8s（22MB）推理需要 700MB+ 会 **OOM 报错**；只能跑 yolov8n 或转 ONNX/NCNN | 预算极限，愿意接受"只能跑小模型+学模型转换" |
| **B. Radxa Zero 3W / 香橙派 Zero 3** | 约 110~270 元（4GB 版约 200；香橙派 Zero3：1GB≈99、2GB≈163、4GB≈199~270） | ARM64 + Debian，pip/apt 与树莓派一致；内存 1.5~4GB 跑 yolov8s 轻松；CPU 比 Zero 2W 强约 2 倍（A55 1.6GHz vs A53 1.0GHz） | GPIO 生态非树莓派（wiringPI 系）；教程少，遇坑要自己折腾 | **视觉组定位**：板子只做摄像头+识别+发结果（串口/网络）→ 迁移成本≈0 |
| **C. 树莓派 4B** | 全新 500+ / 二手 300~450 元 | 终极无缝：比赛就用它，调试代码=比赛代码，零迁移 | 贵；二手注意验机（电源、TF 卡状态） | 预算允许、省心优先 |
| **D. 延迟购买（先不买）** | 0 元 | 当前 90% 工作在笔记本完成，开发板对"识别准确率"贡献为 0；板子价值在"部署验证+小车控制"阶段；过渡可用 WSL/虚拟机（0 成本）学 Debian 命令、pip、SSH | 需自己控制节奏，少一个"时刻摸硬件"的仪式感 | **学习期最优** |

### 3.4 建议优先级（按需选择）
1. **首选：方案 D → 等"上车阶段"再买树莓派 4B**（省下的钱都是学费；避免"过渡板学 3 个月、正式板重学一遍"）
2. **现在就摸真板 + 200 预算 → 方案 B（Radxa Zero 3W 4GB 或香橙派 Zero 3 4GB）**，前提：视觉板只管视觉，不管电机（匹配"我是视觉组"定位）
3. **不建议**：树莓派 Zero 2W（512MB 是新手绕不开的坑）；树莓派 3B+（停产、性能弱、与 4B 软件栈差异反而大）

### 3.5 买板落地清单（避坑提前看）
1. **系统务必 64 位**（Debian/Ubuntu ARM64，或 Raspberry Pi OS 64-bit）——32 位下 pip 包不通用，是"无缝"第一大杀手
2. **装无桌面 Lite 版**（哪怕 4GB 内存）——省内存给 YOLO，SSH 远程操作即可
3. **用 USB 摄像头，别用 CSI 排线**——0 兼容成本，避开 Zero 系列无排线口的问题
4. 部署验证标准：把 best.pt + 脚本拷过去 → 跑一张官方 1280×720 棚拍图 → 看识别是否成功 + 测单张推理时间

---

## 四、双板架构决策（视觉板 + 电机板）

### 4.1 结论：小车可以装两块板——这是行业标准做法
机器人任务分为两层：**上层（大脑）= 算力板**，**下层（小脑）= 控制板（如 STM32）**：

```
┌─────────────────────────────────────┐
│  上层 · 大脑（算力板）                │
│  USB摄像头 + YOLO识别 + 避障决策       │
│  → 结果: "桌子在左前方2米, 走!"        │
│  → 通过串口发给下层一个命令字符串       │
└────────────────┬────────────────────┘
                 │ UART串口 / CAN总线
┌────────────────┴────────────────────┐
│  下层 · 小脑（控制板，如STM32）        │
│  收到命令 → 实时控制电机转速/PID       │
│  读编码器、超声、触碰传感器             │
│  任务: 每秒几百次精确控制轮子           │
└─────────────────────────────────────┘
```

### 4.2 为什么拆两块板（三个原因）
| 原因 | 解释 |
|---|---|
| **实时性** | 电机控制要"每毫秒都算"，YOLO 一次推理几百毫秒；一块板又推理又控制，推理时轮子没人管 → 撞墙。控制=硬实时，视觉=软实时 |
| **稳定性** | 视觉程序崩了，电机板至少还能让车停下来，而不是疯跑 |
| **分工** | 视觉组/控制组各管各的，互不干扰 |

### 4.3 单板全包 vs 双板分工

| | 方案 A：双板分工（推荐） | 方案 B：单板全包（入门简单） |
|---|---|---|
| 硬件 | 树莓派/香橙派（视觉）+ STM32（电机） | 树莓派 4B + 电机驱动模块（TB6612 等） |
| 控制代码 | STM32 用 C，稳定可靠 | Python + gpiozero 直接写 GPIO/PWM |
| 实时性 | ✅ 电机毫秒级响应 | ⚠️ Python 控制轮子，速度快易抖动 |
| 学习成本 | 多学"串口通信"一个知识点 | 少一个知识点，Python 一路走到底 |
| 排障 | 两块板之间找问题 | 只有一块板，简单 |
| 适合谁 | 正规军、控速要求高 | 新手入门、比赛速度慢（1.5~3 米） |

### 4.4 对买板决策的影响（关键）
- **双板架构**下：视觉板职责 = 摄像头 + 推理 + 串口发结果 → 选 Radxa/香橙派等 ARM 板**毫无压力**（约 200 元），与树莓派通用性 100%，"无缝迁移"成立 ✅
- **单板架构**下：必须树莓派 4B（GPIO 生态最顺），买别的板子才是给自己挖坑 ❌

→ **决策依据**：去问学长/队里"底盘是 STM32 还是树莓派直接把电机也接了？"答案直接决定买什么。

### 4.5 最终决定（2026-09-04）
**采用双板架构**。理由：距离正式比赛时间不多了，队伍没有太多时间一起联调，只能**各做各的板块**。

---

## 五、双板合规性与注意事项

### 5.1 规则依据：双板完全合规
规则【技术路线】原文：
> "中心电脑与图像采集设备及机器人底盘相连…通过相机或雷达获取环境信息，将信息发送至中心电脑处理识别，**中心电脑与机器人底盘相连，发送控制指令实现机器人的移动**。"

官方设想就是"上层计算中心 + 底盘执行"两层结构，没有任何规则限制计算单元数量。**需要实测的硬性指标**（唯一可能翻车的物理项）：
- 必须**自主移动**（人不能遥控）
- 高 ≤ 1.8m、重 ≤ 150kg
- **必须能通过 180cm × 70cm 门道** ← 两块板+线材堆上后实测
- 轮子不能碰胶带（起止区）；触碰桌子/道具/障碍物扣 10 分

### 5.2 注意事项（6 条）

#### ⭐ 1. "接口契约"先行（各做各的能成立的前提）
两拨人几乎不一起调试，唯一必须一起做的事：**提前定死通信协议**。建议极简协议（串口 115200 波特率，ASCII 字符串，行尾 \n）：

```
# 视觉板 → 电机板
START\n           # 开始巡检/出发
TARGET x y\n      # 目标点坐标（坐标系需双方商定）
STOP\n            # 停车
CANCEL\n          # 异常中止
RESULT SAVE\n     # 识别完成，车保持静止

# 电机板 → 视觉板
READY\n           # 电机板就绪
ARRIVED\n         # 已到目标区
ERROR xxx\n       # 故障码
```

配套约定：**超时心跳**（如电机板 2 秒收不到任何信息 → 自动停车），否则视觉板死机时小车一直往前撞，既危险又扣分（碰障碍 -10 分/次）。

#### 2. 上电顺序 + 一键启动
规则：只能启动、不得调试代码；只有 2 分钟准备。对策：
- 两块板**同时上电 / 一根命令全部拉起**（start_all 脚本 / 开机自启 / 共用电源开关）
- 启动脚本写死等待逻辑：STM32 先就绪 → 视觉板等 3 秒 → 发 READY → 才开始
- 把"联调问题"转成"顺序问题"，在脚本里提前解决

#### 3. 供电与电气干扰（最容易现场翻车）
- 建议：**一块主电池 → 降压模块分两路**，且**共地**（不共地串口会花屏乱码——让电控学长确认这个概念）
- 电机是大电流负载：电源线、电机线、串口线**分开放**，线尽量短
- 视觉板 USB 供电选好头（供电不足摄像头会断连）
- 车运动时摄像头和 USB 线会震松 → **扎带/热熔胶固定**，USB 线选短而粗的

#### 4. 现场网络（邮件环节）
规则要求"图片结果显示在终端上，同时**现场发送邮件到裁判邮箱**"——视觉板有联网需求：
- 提前测：现场 WiFi 密码？手机热点够不够？
- 用树莓派本地 SMTP 或第三方 API 发附件，**写好后用真实邮箱全流程演练一次**
- 这是 0 分失败点：结果没到裁判邮箱 = 这轮白跑

#### 5. 摄像头设置匹配场地
规则写明"光线色度为**冷光源、50Hz**"（荧光灯/LED 波纹）：
- cv2 里**曝光固定、白平衡关闭**
- 开防闪烁模式（anti-flicker 50Hz）
- 否则画面有条纹或亮度跳变，影响识别置信度

#### 6. 资格认证材料（比赛前一周）
含：队伍介绍、功能展示视频（<100MB）、机器人介绍材料（≥4 页，注明控制板/电机驱动器型号、硬件设备与软件技术）。
双板架构写上去反而**加分**——分工明确的技术方案正是裁判想看到的。

---

## 六、行动清单

| # | 事项 | 时间 |
|---|---|---|
| 1 | 把接口协议文档（第 5.2 节第 1 条）发给电控同学，双方定死：波特率、命令字段、坐标系、心跳规则 | 本周末前 |
| 2 | 视觉部分继续推进：模型 → 拍照识别脚本 → 结果图输出脚本（框+名字+性别标注） | 持续 |
| 3 | 等模型和脚本在笔记本上跑顺，再上板子部署（届时处理：如何把 best.pt + 脚本搬到树莓派/香橙派） | 上车阶段 |
| 4 | 向学长/队里确认：底盘是 STM32 还是树莓派直接接电机 → 决定最终板卡选型 | 尽早 |
| 5 | 5.2 节第 5 条：摄像头 50Hz 防闪烁与曝光固定参数，提前写好并测试 | 场地实测前 |

---

## 七、STM32 学习进展（0904 → 0906）

> 背景：2026-09-04 起从 0 开始学 STM32，为比赛项目做准备（机器人控制板方向）。
> 工具链：**Keil MDK + STM32CubeMX + VSCode**（机器人协会统一安排）；教学视频用的是 **CubeIDE**（两者关系见 7.1）。
> 芯片：STM32F103C8（最小系统板，Cortex-M3）；工程位置：`E:\CubeMXProjectFolder\hello`

### 7.1 工具链关系：为什么不用视频里的 CubeIDE？

```
STM32CubeIDE（视频里的） = CubeMX + 编辑器 + 编译器 + 烧录 打包成一个软件

学长工具链（三件套）：
  STM32CubeMX  → 图形化配置 + 一键生成代码
  Keil MDK     → 编译 + 烧录（.uvprojx）
  VSCode       → 写代码（编辑器，不编译）
```

**核心结论：初始化代码不需要手敲！** 视频里 CubeIDE "Generate Code 一键生成" 的功能，独立版 **CubeMX 同样有**——它是同一个图形化配置工具，只是被 CubeIDE 打包进去了。

### 7.2 用学长工具链生成代码的完整流程

1. **CubeMX 新建工程**：`File → New Project` → 搜索芯片型号（STM32F103C8）→ 双击选中
2. **图形化配置**（和视频一致）：
   - `System Core → RCC` → HSE 选 `Crystal/Ceramic Resonator`（打开外部晶振）
   - `Clock Configuration` → 配时钟树（见 7.3）
   - `Pinout & Configuration` → 点芯片引脚图上的引脚 → 选功能（如 `GPIO_Output`）
3. **Project Manager** → 填工程名/路径 → **Toolchain 选 `MDK-ARM V5`**（这是和视频唯一的差别，视频默认 CubeIDE）→ `GENERATE CODE`
4. 生成结果：`Core/`（main.c 等）、`Drivers/`（HAL 库+CMSIS）、`MDK-ARM/工程名.uvprojx`、`hello.ioc`
5. **Keil**：双击 `.uvprojx` → F7 编译 → 连 ST-Link → F8 烧录
6. **VSCode**：`File → Open Folder` 打开工程根目录 → 编辑 `Core/Src/main.c`

### 7.3 时钟树（Clock Configuration）讲解与标准 72MHz 配置

**时钟树 = 芯片的"心跳分配图"**：一个源头 → 倍频放大 → 分频 → 分给各总线（APB1/APB2）。

| 图上部件 | 作用 |
|---|---|
| HSI RC 8MHz | 芯片内部振荡器，无晶振也能跑，精度一般（默认状态） |
| HSE 4-16MHz | 外部晶振（板子上银色小方块，一般 8MHz），精度高，**正规都用它** |
| LSE 32.768kHz / LSI 40kHz | 低速时钟，只给 RTC/看门狗，**新手不管** |
| PLL Source Mux | 选 HSE 还是 HSI 去倍频的"开关" |
| PLL (X2) | 倍频器：把 8MHz 放大（×9 = 72MHz） |
| System Clock Mux | 选 PLL 输出作为 SYSCLK（CPU 主频） |
| AHB/APB Prescaler | 分频器，给不同"部门"，各有上限 |

**标准配置步骤（F103 经典 72MHz）**：
1. 先在 `Pinout & Configuration → RCC` 把 HSE 设为 `Crystal/Ceramic Resonator`（否则 HSE 是灰的，选不了！）
2. 回 `Clock Configuration`：PLL Source Mux 选 **HSE** → PLL 倍频 **X9**（8×9=72）→ System Clock Mux 选 **PLLCLK** → APB1 Prescaler **/2**（72÷2=36，APB1 上限 36MHz）
3. 检查：SYSCLK=72、HCLK=72、APB1=36、APB2=72，**无红字** = 成功（红字 = 配置非法）

### 7.4 USER CODE 保护区（必记）

main.c 中所有 `USER CODE BEGIN xxx / USER CODE END xxx` 之间的代码是**保护区**：
- 自己写的代码必须放在里面
- CubeMX 重新 Generate Code 时，**保护区外的代码会被覆盖**，保护区内的保留

### 7.5 Keil 编译与烧录（编译不需要硬件）

- **编译（F7）**：检查代码语法/拼写，**不需要 ST-Link**——ST-Link 未到也能做 ✅
- **烧录（F8）**：把程序写进芯片，**需要 ST-Link**（快递到后再做）

main.c 结构认知（CubeMX 自动生成，只需改两个 USER CODE 区域）：
```c
HAL_Init();                    // HAL 库初始化
SystemClock_Config();          // 时钟配置（刚配的 72MHz）
MX_GPIO_Init();                // 引脚初始化
/* USER CODE BEGIN 2 */        // ← 主循环前执行一次
/* USER CODE END 2 */
while (1) {
  /* USER CODE BEGIN 3 */      // ← 无限循环（LED 闪烁代码放这）
  /* USER CODE END 3 */
}
```

LED 闪烁示例（PA0 为 GPIO_Output 时）：
```c
HAL_GPIO_TogglePin(GPIOA, GPIO_PIN_0);  // 翻转 PA0
HAL_Delay(500);                          // 延时 500ms
```

### 7.6 已踩的坑：Keil 报 "Default Compiler Version 5 not available"

**原因**：CubeMX 老模板默认指定 **AC5**（armcc，老编译器），新版 Keil（MDK 5.36+）默认只带 **AC6**（armclang），AC5 需单独装。
**解决**：工具栏"魔术棒"（Options for Target，Alt+F7）→ `Target` 标签页 → `ARM Compiler` 下拉框 → 选 **`Use default compiler version 6`**（或具体 V6.xx）→ OK → F7。
**AC6 编译 CubeMX 标准工程完全兼容**，可能多几条警告，不影响运行。

### 7.7 已踩的坑：VSCode 满屏"未定义标识符"红波浪线

**原因**：IntelliSense 不知道 HAL 库头文件在哪（Keil 知道，VSCode 不知道）——**是编辑器配置问题，不是代码错误**。
**验证**：以 Keil 编译结果为准（0 Error = 代码没问题）。
**解决**：`Ctrl+Shift+P` → `C/C++: Edit Configurations (JSON)` → 配置 `.vscode/c_cpp_properties.json`：

```json
{
  "configurations": [{
    "name": "STM32",
    "includePath": [
      "${workspaceFolder}/**",
      "${workspaceFolder}/Core/Inc",
      "${workspaceFolder}/Drivers/STM32F1xx_HAL_Driver/Inc",
      "${workspaceFolder}/Drivers/STM32F1xx_HAL_Driver/Inc/Legacy",
      "${workspaceFolder}/Drivers/CMSIS/Device/ST/STM32F1xx/Include",
      "${workspaceFolder}/Drivers/CMSIS/Include"
    ],
    "defines": ["STM32F103xB", "USE_HAL_DRIVER"],
    "compilerPath": "C:/Keil_v5/ARM/ARMCC/bin/armcc.exe",
    "cStandard": "c11",
    "intelliSenseMode": "gcc-arm"
  }],
  "version": 4
}
```

### 7.8 工程结构与 git 管理规范

**工程结构**（E:\CubeMXProjectFolder\hello）：
```
hello/
├── hello.ioc              # CubeMX 配置（灵魂文件）
├── .mxproject             # 生成元数据
├── Core/Inc|Src/          # 用户代码（main.c 等）
├── Drivers/               # HAL 库 + CMSIS（第三方）
└── MDK-ARM/               # Keil 工程（.uvprojx、startup.s）+ 编译产物
```

**git 该追踪 vs 不追踪**（已落地到 .gitignore）：

| ✅ 追踪 | ❌ 忽略（.gitignore） |
|---|---|
| hello.ioc、.mxproject | MDK-ARM/hello/（.o/.axf/.hex/.map/.lnp/.htm/.dep 等编译产物） |
| Core/ 全部 | MDK-ARM/*.uvoptx、*.uvguix*（本机配置） |
| MDK-ARM/hello.uvprojx、startup_stm32f103xb.s | MDK-ARM/DebugConfig/、RTE/ |
| Drivers/（HAL + CMSIS 核心） | *.bak、Thumbs.db、Desktop.ini |

**Drivers/ 三种策略**：A. 全提交（推荐新手/比赛，克隆即编译）｜B. 只留用到的（干净但易手滑删错）｜C. 全忽略靠 .ioc 重新生成（最小但队友需 CubeMX）。

### 7.9 git 状态与仓库体检结果（0906）

- `git status` 显示 `nothing to commit, working tree clean` = **正常**：已全部提交且无改动；验证用 `git log --oneline -5` / `git ls-files` / `git remote -v`
- **仓库体检结果**：共跟踪 **911 个文件**
  - `hello.ioc` ✅ 在仓库；MDK-ARM 只跟踪 `.uvprojx` + `startup.s` ✅（**gitignore 生效，编译产物零混入**）
  - CMSIS 占 **767 个**（84%），其中 **DSP 535 + NN 76 + RTOS/docs ≈ 620 个是 CubeMX 顺手拷贝的无用库**（F103 点灯用不上）
  - 结论：约 290 个真有用 + 620 个冗余；**仓库可清理但暂不清理**（先跑通 LED，后减肥；清理命令 `git rm -r --cached Drivers/CMSIS/DSP ...`，本地文件保留）
- 已推送到远程（origin/main）

### 7.10 Windows 下没有 grep（已踩）

Linux 的 `grep` 在 PowerShell 不存在，替代方案：
```powershell
git ls-files | Select-String "hello.ioc"   # PowerShell 正统（等价 grep）
git ls-files | findstr hello.ioc           # cmd 传统
git ls-files "*hello.ioc"                  # git 自带通配符（最省事）
git ls-files | grep hello.ioc              # 需要 Git Bash 环境
```

### 7.11 当前状态与下一步

- **已就绪**：CubeMX 生成工程（72MHz 时钟 + 引脚配置）、Keil 编译链（AC6 已切换）、VSCode 配置完成、git 仓库管理完成
- **待办**：
  1. ST-Link 到货 → Keil 烧录 → 点亮 LED（验证 72MHz 时钟 + GPIO 配置是否正确）
  2. Keil 编译确认 0 Error（AC6 切换后尚未编译验证）
  3. LED 跑通后：再学 UART（串口）—— 为视觉板↔控制板接口协议（第 5.2 节）做铺垫

---

*文档生成：2026-09-06 · 视觉组 · 与 AI 顾问讨论整理（0904 版基础上更新）*
